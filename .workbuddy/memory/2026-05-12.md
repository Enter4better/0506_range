# 2026-05-12 工作日志

## 小红书 MCP 接入（重大突破）

### 完成事项
- 成功接入 xiaohongshu-mcp 服务（`D:\软件安装\xiaohongshu-mcp-windows-amd64`）
- 解决了 MCP 协议的关键问题：**`Mcp-Session-Id` 会话机制**
  - MCP 服务器在 HTTP 响应头中返回 `Mcp-Session-Id`
  - 所有后续请求必须在 HTTP 请求头中带上这个 Session ID
  - 这是非标准 MCP 行为，是此服务器的专属设计
- 成功调用 `search_feeds` 搜索小红书笔记
- 收集了 27 条点赞数超过 1000 的「全屋定制儿童房」相关笔记
- 整理并导出为 Excel：`D:/0503/AI_range/小红书儿童房定制笔记_点赞1000+.xlsx`

### 技术发现
- MCP 服务器地址：`http://localhost:18060/mcp`（已自动启动）
- Session ID 每次 `initialize` 调用后都会变化
- 服务器不支持标准 MCP 的 `initialized` 通知
- 可用工具共 13 个（认证/发布/搜索/互动/用户）

### 已创建 Skill
- 路径：`C:/Users/王山而/.workbuddy/skills/xiaohongshu-mcp/SKILL.md`
- 包含完整的协议说明和示例代码

### 搜索关键词汇总（儿童房定制相关）
`全屋定制儿童房`、`儿童房定制`、`儿童房设计`、`儿童房装修`、`定制儿童房`

### 文件路径
- MCP 程序：`D:/软件安装/xiaohongshu-mcp-windows-amd64/`
- Excel 输出：`D:/软件安装/小红书儿童房定制笔记_点赞1000+.xlsx`（用户指定输出目录）
- JSON 中间文件：`D:/0503/AI_range/xhs_notes.json`

### 用户偏好
- **文件默认输出目录**：`D:\软件安装\`

---

## 登录权限修复

### 问题
- admin 用户登录后右上角不显示用户信息，或显示不正确
- 普通用户也能看到"AI靶场"菜单（应该只有管理员可见）

### 根因
- **角色值不一致**：Login.vue fallback 用 `'管理员'`（中文），App.vue/router 用 `'admin'`（英文）
- 数据库中 admin 的 role 是 `'admin'`（英文），与前端判断不匹配

### 修复内容
1. **Login.vue 第256行**：fallback 角色从 `'管理员'` 改为 `'admin'`
2. **App.vue 第50行**：角色显示从 `user.role || '用户'` 改为 `user.role === 'admin' ? '管理员' : '普通用户'`

### 验证
- 后端 /api/health 正常返回
- 需刷新页面后重新登录测试

---

## 登录权限与用户管理修复

### 新增修复内容

#### 1. logout() 函数完善
**文件**: `frontend/src/App.vue`
- 原来只清除 `cyber_user`
- 现在同时清除 `cyber_user`、`token`、`cyber_remember`

#### 2. 靶场操作权限控制
**文件**: `frontend/src/views/EnvManage.vue`
- 启动按钮：添加 `v-if="isAdmin"`
- 停止按钮：添加 `v-if="isAdmin"`
- 保持删除按钮的 `v-if="isAdmin"` 不变

#### 3. Dashboard 靶场管理卡片权限
**文件**: `frontend/src/views/Dashboard.vue`
- 在 script 中添加 `isAdmin` computed 属性
- 靶场管理卡片添加 `v-if="isAdmin"`，仅管理员可见

#### 4. Token 过期 401 处理
**文件**: `frontend/src/utils/request.js`
- 响应拦截器添加 401 状态码处理
- 401 时自动清除 localStorage 并跳转登录页

### 待用户确认
- 普通用户 Dashboard 的靶场管理卡片位置是否需要替换为其他内容（拓扑/日志）？

---

## Dashboard 权限和布局优化

### 修改内容

#### 1. Tab 切换栏权限控制
**文件**: `frontend/src/views/Dashboard.vue`
- Tab 切换栏添加 `v-if="isAdmin"`，仅管理员可见
- 普通用户不显示 AI/手动 切换，直接显示手动配置板块

#### 2. activeSection 初始化
- 管理员默认 `activeSection = 'ai'`
- 普通用户默认 `activeSection = 'manual'`

#### 3. 手动配置板块显示条件
- 从 `v-show="activeSection === 'manual'"` 改为 `v-show="activeSection === 'manual' || !isAdmin"`

#### 4. 卡片响应式布局
- 第一行功能卡片统一使用 `:xs="24" :sm="12" :md="8"` (3列)
- 第二行攻防卡片使用 `:xs="24" :sm="12" lg="6"` (2列)

#### 5. 普通用户 Dashboard 布局
- 第一行：日志卡片 + 拓扑卡片
- 第二行：攻击模拟卡片 + 防御配置卡片

### 待实现
- 用户管理功能（后端API + 前端页面）

---

## 论文修改（毕设初稿）

### 完成事项
- 读取并分析论文初稿 `D:/毕设/文档/初.docx`
- 验证参考文献真实性，发现大量不可查证的文献（[2][3][6][10][12][14-18][21-27]等均无法验证）
- 完成论文全面修改，写入 `D:/毕设/文档/论文_修改后.md`

### 文献替换方案
原不可验证文献的替代：
- [2] AutoPentest → PentestAgent (arXiv:2411.05185, 2024)
- [3] AutoVuln → LLM-Driven Automation in Vulnerability Management (OJST, 2024)
- [6] Seo(Sensors) → 基于Docker容器的网络安全实验平台 (2025)
- [10] CyberAgent → HackSynth (arXiv:2412.01778, 2024)
- [17] RangeGPT → Dynamic Cyber Ranges (arXiv:2604.24184, 2026)
- [19] Liang(ACL) → Cybench Zhang et al. (arXiv:2408.08926, 2024)
- [21] MultiAgent-CTF → CFA-Bench (arXiv:2506.17644, 2025)
- [23] ContainerGuard → IEEE Access 2020版本（作者/年份不符）
- 其他 → 替换为可查证的真实文献

### PlantUML架构图生成
已生成以下6个图的PlantUML代码文件：
- `PlantUML_系统架构图.puml` - 三层架构（表示层/服务层/基础设施层）
- `PlantUML_多智能体协同流程图.puml` - EnvAgent/AttackAgent/DefenseAgent与Orchestrator
- `PlantUML_数据库ER图.puml` - users/environments/attack_logs/defense_alerts/defense_rules/training_examples
- `PlantUML_任务路由流程图.puml` - 9种任务类型的参数路由逻辑
- `PlantUML_WatchdogService架构图.puml` - 注册-巡检-告警三阶段
- `PlantUML_KillChain攻击流程图.puml` - 6阶段攻击生命周期
- `PlantUML_防御响应流程图.puml` - DefenseAgent处理流程

### 主要修改内容
1. 删除所有编辑提示文字（"上面内容要适当扩充"、"具体怎么实现的？"等）
2. 重写第六章总结与展望（消除结构混乱问题）
3. 精简重复性语句，降低AI率
4. 修正LaTeX公式（攻击成功率模型、防御拦截率模型）
5. 所有文字保留源自项目实际的内容
6. 重新整理参考文献列表（共29条，均为真实可查证）

---

## 用户管理功能实现

### 后端 API
**文件**: `backend/routes/auth.py`
- `GET /api/auth/users` - 获取用户列表（已有）
- `PUT /api/auth/users/<user_id>` - 更新用户（角色、邮箱）
- `DELETE /api/auth/users/<user_id>` - 删除用户

### 前端页面
**文件**: `frontend/src/views/UserManage.vue`（新建）
- 用户列表展示（表格）
- 编辑用户角色和邮箱
- 删除用户（保护自己不能删除自己）

### 路由配置
**文件**: `frontend/src/router/index.js`
- 新增路由 `/users`，`meta: { requiresAdmin: true }`
- 导航栏新增"用户管理"菜单，`adminOnly: true`

### API 测试
- `GET /api/auth/users` 返回正常，包含 admin 和 user 两个用户
