# 2026-05-13 工作日志

## 用户权限修复

### 问题
- admin 和 user 切换登录后，页面显示混乱（身份错误）
- 普通用户退出登录后，右上角仍显示登录样式
- Dashboard 被 keep-alive 缓存导致状态不刷新

### 修复内容

#### 1. keep-alive 排除 Dashboard
**文件**: `frontend/src/App.vue`
- Dashboard 被排除在 keep-alive 缓存之外，确保每次访问都重新挂载

#### 2. 路由监听重新检查登录状态
**文件**: `frontend/src/App.vue`
- 添加 `watch(() => route.path)` 监听
- 导航到 `/` 时重新调用 `checkLogin()`

#### 3. isAdmin 改为函数式
**文件**: `frontend/src/views/Dashboard.vue`
- `isAdmin` 从 `computed` 改为普通函数，每次调用都重新读取 localStorage
- 模板中使用 `isAdmin()` 调用

---

## Dashboard 布局优化

### 管理员页面
- 第一行三个卡片（靶场管理 + 日志 + 拓扑）：各占 1/3 宽度 (lg="4")
- 第二行两个卡片（攻击模拟 + 防御配置）：各占 1/2 宽度 (lg="6")
- 两行总宽度一致

### 普通用户页面
- 第一行两个卡片（日志 + 拓扑）：各占 1/2 宽度 (lg="6")
- 第二行两个卡片（攻击模拟 + 防御配置）：各占 1/2 宽度 (lg="6")

---

## 今日攻击统计

### 后端修改
**文件**: `backend/routes/stats.py`
- `/stats/overview` 返回数据新增：
  - `today_attacks`：今日攻击次数
  - `today_alerts`：今日拦截次数

### 前端修改
**文件**: `frontend/src/views/Dashboard.vue`
- 统计卡片新增"今日攻击"和"总攻击"两个独立卡片
- 今日攻击：使用 `statsData.today_attacks`
- 总攻击：使用 `statsData.attacks`
- 拦截次数：优先使用 `statsData.today_alerts`

---

## 用户管理增强

### 新增功能
**文件**: `frontend/src/views/UserManage.vue`

1. **增加用户**
   - 弹窗表单：用户名、邮箱、密码、确认密码、角色
   - 表单验证：密码长度、大小写、数字要求
   - 调用 `/auth/register` API

2. **修改密码**
   - 操作列新增"改密"按钮
   - 弹窗表单：新密码、确认密码
   - 调用 `/auth/users/<id>` PUT API

3. **操作按钮宽度调整**
   - 操作列宽度从 200 增加到 280
   - 三个按钮：编辑、改密、删除

### 后端修改
**文件**: `backend/routes/auth.py`
- `update_user` 函数支持 `password` 字段更新
- 添加密码强度验证

---

## 相关文件修改汇总

| 文件 | 修改 |
|------|------|
| `frontend/src/App.vue` | keep-alive 排除 Dashboard，添加路由监听 |
| `frontend/src/views/Dashboard.vue` | isAdmin 改为函数，布局优化，添加总攻击统计 |
| `backend/routes/stats.py` | 添加今日攻击/拦截统计 |
| `frontend/src/views/UserManage.vue` | 添加增加用户、修改密码功能 |
| `backend/routes/auth.py` | update_user 支持密码修改 |

---

## Dashboard 布局修正（2026-05-13 中午）

### 问题
- 管理员手动配置靶场第一行三个卡片宽度过窄（`:lg="4"` 只占 1/6）
- 普通用户第一行两个卡片 `:lg="6"` 只占 1/4
- 第二行攻击模拟/防御配置 `lg="6"` 绑定方式错误
- 顶部"拦截次数"卡片需要删除

### 修复
**文件**: `frontend/src/views/Dashboard.vue`
- 删除"拦截次数" StatCard
- 管理员第一行三个卡片：`:lg="4"` → `:lg="8"`（各占 1/3）
- 普通用户第一行两个卡片：`:lg="6"` → `:lg="12"`（各占 1/2）
- 第二行攻击模拟/防御配置：`lg="6"` → `:lg="12"`（各占 1/2）
- 两行总宽度一致，每个卡片等宽

---

## 用户管理支持修改用户名

### 前端修改
**文件**: `frontend/src/views/UserManage.vue`
- 编辑对话框用户名输入框去掉 `disabled`
- `handleSave` 中 PUT 请求增加 `username` 字段

### 后端修改
**文件**: `backend/routes/auth.py`
- `update_user` 增加 username 更新逻辑
- 包含长度校验（3-20 字符）和重名检查

---

## user 靶场页面权限

**文件**: `frontend/src/views/EnvManage.vue`
- 已存在 `v-if="isAdmin"` 控制：创建靶场、清理全部、启动/停止/删除按钮
- 普通用户进入 `/env` 只能查看列表和访问靶场，不可管理

---

## 论文图表优化（2026-05-13 下午）

### 问题
- 方框过大，字体太小，留白太多
- 箭头衔接不流畅，有错位
- 文字位置错乱

### 优化内容
**文件**: `D:/毕设/文档/generate_diagrams.py`

1. **系统总体架构图** - 紧凑三层布局，减小方框，增加字体
2. **多智能体协同流程图** - 流畅曲线箭头，用户→Orchestrator→三个Agent→Docker/SQLite
3. **数据库ER图** - 紧凑表格，清晰的关系线
4. **Kill Chain攻击流程图** - 两行布局，阶段3→阶段4用折线连接
5. **WatchdogService架构图** - 三阶段+四监控项布局
6. **防御响应流程图** - 垂直流程，箭头流畅衔接

### 生成的文件
- `图1_系统总体架构图.png` (105KB)
- `图2_多智能体协同流程图.png` (98KB)
- `图3_数据库ER图.png` (94KB)
- `图4_KillChain攻击流程图.png` (137KB)
- `图5_WatchdogService架构图.png` (113KB)
- `图6_防御响应流程图.png` (61KB)
