背景
不同于静态和预设的传统网安靶场，AI驱动的靶场自动生成系统利用多个AI智能体，根据训练目标，自动、实时地构建一个高度仿真的网络环境（靶场），并在其中持续生成、执行、调整复杂的攻防演练，减少人工干预。

内容与要求（包括规定阅读的文献、应完成的程序、图纸、实验、说明书等）
1、环境管理Agent：负责靶场底层的资源编排。接收场景描述后，自动在云基础设施（采用Docker）上创建虚拟机、容器、网络设备（软路由、防火墙），并配置系统、部署应用服务（如Web服务器、数据库）。
2、模拟攻击Agent：利用环境中的漏洞达成特定目标（如获取域控权限、窃取数据）。需要给出共多少种攻击类型。同时防御需要覆盖所有攻击。
3、模拟防御Agent：检测和阻止攻击。自动部署安全工具（比如ELK/Splunk日志分析、WAF、EDR等，不限制）、分析日志（采用watchdog）中的异常行为、生成告警、甚至自动响应（如封禁IP、隔离主机）。
4、查阅相关的文献资料12篇以上，翻译其中的2篇外文文献，按要求撰写文献综述和开题报告。
5、实现靶场的自动生成系统，并撰写规范的毕业设计文档（论文）。