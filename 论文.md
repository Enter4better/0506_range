AI驱动的靶场自动生成系统


摘要：传统网络安全靶场在建设和维护过程中，存在部署周期较长、场景更新不够灵活、依赖人工配置等问题。针对这些问题，本文设计并实现了一套基于多智能体和大语言模型的靶场自动生成系统。系统采用Flask+Vue前后端分离架构，以Docker容器作为底层运行环境，主要包括环境管理、攻击模拟、防御分析三个智能体，并通过Orchestrator编排器进行统一调度。
在系统实现上，本文基于DeepSeek API设计了多任务模型路由机制，针对语义解析、配置生成、攻击分析、防御决策等不同任务设置差异化的模型和参数。同时，系统引入历史成功案例作为少样本提示示例提高靶场配置生成的稳定性。针对大语言模型可能生成非法镜像或不完整配置的问题，采用镜像白名单和代码层补全机制进行约束。此外，本文还实现了WatchdogService后台监控服务，对宿主机CPU、内存、磁盘和Docker容器状态进行周期性检查。
测试结果表明，在镜像已缓存的条件下，系统能够在秒级时间完成靶场环境部署，并支持SQL注入、XSS、端口扫描等16类常见攻击场景的模拟演练。整体来看，该系统降低了轻量级网络安全训练环境的搭建门槛，为教学和基础攻防演练场景提供了一种可行的自动化实现方案。
关键词：信息安全；AI agent；靶场；CTF；LLM
中图分类号：TP393





AI-Driven Target Range Automatic Generation System

Abstract:Traditional cybersecurity ranges typically suffer from long deployment cycles,inflexible scenario updates,and heavy reliance on manual configuration.To address these limitations,this paper designs and implements an automated range generation system based on multi-agent collaboration and large language models.
The system adopts a Flask+Vue front-end and back-end separated architecture with Docker containers as the underlying runtime environment. It consists of three specialized AI agents--responsible for environment management,attack simulation,and defense analysis respectively--and is coordinated by a central Orchestrator.On the LLM side, a multi-task model routing mechanism is built based on the DeepSeek API,assigning distinct models and inference parameters to different tasks such as semantic parsing,configuration generation,attack planning and defense decision-making.To improve configuration stability,the system injects historically successful deployments as few-shot examples into prompts.To prevent LLMs from generating invalid images or incomplete configurations,an image whitelist combined with a code-level auto-patching mechanism is applied.In addition,a WatchdogService daemon is implemented to periodically monitor host CPU,memory,disk usage and Docker container status.
Test results show that,under pre-cached image conditions,the system can complete range deployment within seconds and supports simulation exercises across 16 common attack scenarios including SQL injection and XSS.Overall, the system significantly lowers the barrier to building lightweight cybersecurity training environments，offering a practical automated solution for teaching and basic attack-defense training scenarios.
Keywords:Information Security;AI Agent;Range;CTF;LLM
Classification:TP393


目   次

摘要...................................................................Ⅰ
目次...................................................................Ⅲ
1 绪论	1
1.1 研究背景	1
1.2 研究意义	1
1.2.1 工程层面	1
1.2.2 训练层面	1
1.2.3 研究层面	2
1.3 研究目标	2
1.4 研究与创新	2
1.4.1 多智能体协同的靶场自动生成架构	2
1.4.2 自适应模型路由与少样本学习。	2
1.4.3 容器监控与双重配置保障。	2
2 相关工作与技术综述	3
2.1 网络安全靶场研究现状	3
2.2 大语言模型在网络安全领域的应用	3
2.3 多智能体设计	4
2.4 容器化与系统监控技术	4
3 系统总体设计	5
3.1 设计原则	5
3.2 系统总体架构	5
3.2.1 基础设施层	5
3.2.2 服务层	5
3.2.3 表示层	6
3.3 多智能体协同设计	7
3.4 数据库设计	8
4 系统关键模块实现	9
4.1 LLM模块	9
4.2 AI靶场生成与少样本学习	10
4.2.1 镜像白名单与配置约束	10
4.2.2 少样本学习机制	11
4.2.3 容器创建与状态同步	12
4.3 攻防模拟实现	12
4.3.1 攻击部分	12
4.3.2 防御部分	14
4.3.3 沙箱攻击执行模式	16
4.4 后台WatchdogService监控服务	18
4.4.1 架构设计	15
4.4.2 系统资源监控	16
4.4.3 与防御体系的协同	17
4.4.4 告警数据对外接口	17
4.5 前端关键实现	17
4.5.1 AI场景生成页	17
4.5.2 靶场管理页	18
4.5.3 攻击面板	18
5 测试与评估	19
5.1 测试环境	19
5.2 功能测试	19
5.2.1 靶场自动生成测试	19
5.2.2 攻防演练测试	21
5.2.3 WatchdogService测试	22
5.3 性能评估	23
5.3.1 靶场生成时间	23
5.3.2 API响应时间	24
5.3.3 少样本学习效果	25
5.4 局限性分析	26
6 总结与展望	27
6.1 研究总结	27
6.1.1 多智能体协同的靶场自动生成架构。	27
6.1.2 自适应模型路由与少样本学习。	27
6.1.3 容器监控与配置可靠性保障。	27
6.2 未来工作	28
6.2.1 Kubernetes多节点扩展	28
6.2.2 强化学习驱动的攻防对抗	28
6.2.3 知识图谱增强的场景生成。	28
参考文献	29

1绪论
1.1研究背景
近年来，网络攻击事件持续活跃。人工智能技术飞速发展和应用广泛的同时，也给网络空间安全带来了新的挑战，如网络攻击越来越智能化，大规模网络攻击越来越频繁，网络攻击的隐蔽性越来越高而对抗博弈越来越强，重要数据越来越容易被窃取等[1]。奇安信威胁情报中心发布的《网络安全威胁2024年度报告》显示，2024年全年境内政企机构数据泄露事件达156起，全球累计泄露数据471.6亿条，同比增长354.3%[2]。在这种背景下，单纯依赖静态规则和人工经验的防御方式已经难以覆盖全部网安场景，安全人员需要更真实的训练环境来提升应对能力。
网络安全靶场是一种用于模拟真实攻防对抗环境的训练基础设施。但在实际建设过程中，传统靶场存在搭建周期长、场景更新困难、运维成本较高等问题。针对这些问题，大语言模型可以用于理解用户的自然语言需求并辅助生成靶场配置；Docker容器则可以提供相对轻量的环境隔离和快速部署能力。将LLM的语义理解能力与Docker的容器部署能力结合起来，构建一个能够根据用户描述自动生成靶场环境的系统，在工程实现上是有一定可行性的。
1.2研究意义
1.2.1工程层面
从工程实现角度看，LLM Agent已经开始被用于CTF、渗透测试辅助等网络安全任务中。Cybench构建了一个包含40个专业级CTF任务来评估LLM安全能力的基准框架，验证了语言模型在命令执行与结果分析上的可行性[3]。和这个研究不同的是本文不注重评测模型能力，研究重点放在自然语言需求到Docker靶场环境自动部署的实现上。
另外，当把多智能体协同机制引入进来后，像需求解析、环境编排、攻击模拟、防御分析这些靶场生命周期的各个阶段，就分别由专门的AI Agent来负责。如果整个流程实现了自动化，搭建一个靶场所花的时间就被压缩到了分钟级。
1.2.2训练层面
传统固定靶场在场景复用和个性化方面存在一定限制。本文系统允许用户通过自然语言描述生成不同类型的靶场环境，有助于根据训练目标快速构建Web漏洞、数据库服务、容器安全等不同的场景，从而提升教学和基础演练的灵活性。
1.2.3研究层面
本系统探索了LLM在靶场配置生成中的应用方式，并结合历史成功案例进行少样本提示增强。该方法不涉及模型参数训练，而是通过上下文示例提升配置生成的稳定性，为资源受限条件下的大模型应用提供了一种轻量级实现思路。
1.3研究目标
针对传统网络安全靶场在建设和运维中存在的以下问题：部署周期长、场景固化、依赖人工配置，设计实现了一套基于多智能体与大语言模型的靶场自动生成系统。主要研究目标包括：
（1）实现基于自然语言描述的靶场需求解析与自动部署；
（2）设计多智能体协同机制，完成攻击模拟与防御响应的流程串联；
（3）在保证基本可用性的前提下，降低靶场搭建的技术门槛与时间成本。
1.4研究与创新
1.4.1多智能体协同的靶场自动生成架构
本文一共设计了三个专用AI智能体，然后由Orchestrator编排器来统一调度，形成“环境创建→攻击执行→防御响应”的执行流程。用户输入自然语言描述后系统就可以完成从需求解析一直到容器运行的完整流程，用户自己无需具备Docker配置知识。
1.4.2多任务模型路由与少样本学习。
本文构建了一个多任务模型路由器，这个路由器会根据语义解析、配置生成、攻击分析等不同的任务类型，动态地分配Chat模型或者Reasoner模型，并且给它们配上差异化温度参数。本文还加入了一套基于关键词频率匹配的轻量级少样本学习机制，这套机制会从那些已经成功部署的历史配置里面，自动地去积累训练样本，这样一来，大语言模型对靶场需求的解析准确率就会随着使用次数的增加而逐步提升了。
1.4.3容器监控与双重配置保障。
针对大语言模型在生成配置的时候有可能输出非法镜像名这个实际问题，采用“Prompt约束+代码层自动修补”的双重保障机制提高生成配置的可部署性。另外还开发了WatchdogService这个后台守护服务，它会对宿主机资源和Docker容器的状态做周期性巡检，一旦资源超出了设定的阈值就会自动触发告警，这样一来就进一步提升了平台的稳定性。

2相关工作与技术综述
2.1网络安全靶场研究现状
靶场技术走到今天，大致经历了三个演变阶段。最早的是物理机靶场，真实性高但是成本也高，导致多数机构都负担不起。后来出现了以VMware、KVM为代表的虚拟化靶场，靠虚拟机的快照实现了场景的复现，不过启动慢、资源占用大的问题一直都没能得到根本解决。再往后就是容器化方案，Docker和Kubernetes等容器化技术提供了更轻量的环境隔离方式，已经成为靶场轻量化部署的重要技术路线之一。
Yamin等人对网络靶场和安全测试床的场景、功能、工具和架构进行了综述，指出靶场可以用于安全训练、测试和评估等任务[4]。国内也有研究关注大模型对网络靶场智能化的影响，如蔡晶晶团队从演进方向的角度指出大模型可以在场景生成、攻防演练和训练评估等方面为靶场提供智能化支撑。本文在此方向上进一步落地，设计实现了一个面向真实容器部署的靶场自动生成系统[5]。此外，CyExec*基Docker容器实现了高性能网络安全靶场，通过场景随机化机制提升训练多样性，存储开销仅为虚拟机方案的1/60，场景可复现率超过99%[6]。但该方案还是需要依赖人工编写场景模板，缺乏自动化配置生成能力，这也是本文重点解决的问题。
本文主要是在这些研究方向的基础上，结合Docker和LLM实现一个轻量级的靶场自动生成系统。
2.2大语言模型在网络安全领域的应用
在网络安全领域，LLM已从最初的威胁情报提取工具，逐步扩展到可以进行漏洞扫描自动化和自主攻击规划等更复杂的任务。但已有研究指出，大语言模型在增强传统安全方法的同时也可能带来隐私泄露和输出不可控等安全问题[7]。因此，在将大语言模型引入安全系统时需要结合规则约束、人工校验或代码层校验机制来提高系统可靠性。
PentestAgent[8]将LLM Agent引入自动化渗透测试，把测试流程拆解成信息收集、漏洞利用等子任务，分别由专用Agent处理，对任务拆解和攻击步骤规划具有一定参考价值。江颉等人提出基于大语言模型的多智能体自动渗透测试框架也说明多智能体方法在安全测试任务中是有应用价值的[9]。不同的是本文重点不在于实现完全自主渗透测试，而是将LLM用于靶场需求解析、配置生成、攻击步骤模拟和防御建议生成等环节。
系统最后选用的是DeepSeek系列模型。拿DeepSeek-V3来说，它在代码基准测试上面的表现已经达到了GPT-4o的水平，但是API调用成本却明显低不少。DeepSeek-R1通过强化学习激励模型自主发展推理能力，实验表明它在多步逻辑推理任务上的表现显著优于同规模的Chat模型[10]。因此本文将任务分为两类：对于靶场配置生成、语义解析等需要快速、确定性输出的任务，选用Chat模型；对于攻击分析、防御决策等需要综合多角度信息进行深度推理的任务就选用Reasoner模型，并配置接近零的温度参数以保证判断的稳定性。此外还对V3和V4-Flash做了对比测试，结果显示这两款模型在系统的靶场配置生成任务上质量差不多。考虑到V3免费且系统面向教学场景，调用频率比较高，所以最后选了DeepSeek Chat作为主力模型来承担配置生成、语义理解这类任务。
2.3多智能体设计
在网络安全场景下的多智能体优势在于可以把攻防双方建模为相互对立的智能体来模拟真实对抗过程，主要思路是把一个复杂任务拆分成多个职责相对明确的子任务。在自动化渗透测试研究中，PentestAgent将渗透测试过程拆分为多个环节[8]，对本文的攻击模拟模块有一定参考意义。
本文没有采用完全自治的多智能体通信方式，而是设计了一个Orchestrator编排器，由它统一调度EnvAgent、AttackAgent和DefenseAgent。这样做的原因是，完全依赖LLM自主协作在调试时不太稳定，而本文作为一个工程实现型系统，更需要保证流程可控、错误可定位。因此，本文采用了规则编排和Agent分工结合的方式。
2.4容器化与系统监控技术
Docker容器启动较快、环境一致性较好、资源隔离也比较清晰，因此适合用于轻量级靶场环境的搭建。已经有研究基于Docker技术设计网络安全靶场，通过容器化方式提升靶场部署、恢复和扩展的灵活性[11]。在漏洞靶场建设中，也有研究利用Docker快速部署动态靶机来降低环境搭建成本[12]。本文也采用Docker作为底层环境，通过Docker SDK对容器生命周期进行自动化管理。不同的是出于平台稳定性与镜像可控性的考虑，选用经过验证的标准官方镜像，漏洞场景的训练效果通过AttackAgent的攻击仿真层来体现，而非依赖镜像中的真实漏洞。
在系统监控方面，本文采用轻量级Watchdog思路，通过独立守护线程周期性检查宿主机CPU、内存、磁盘以及容器状态。该方案不依赖复杂监控平台，适合本文目前单机部署的应用场景。

3系统总体设计
3.1设计原则
职责分离原则：前端只管交互与展示，后端负责业务逻辑，LLM主导语义理解跟内容生成，Docker则用来做基础设施创建。各层之间通过明确定义的接口来通信，这样一来职责规划就很清晰，也能避免因为强耦合而可能带来的联动故障。
状态同步原则：系统把Docker Engine的实际运行状态当作靶场状态的唯一真实来源，数据库只作为元数据的缓存来用。每次查询靶场列表的时候，系统会先从Docker API拿到实时的容器状态，然后再跟数据库里的记录做对比和同步，这样才能保证前端展示出来的内容跟底层的实际情况是一致的。这个设计还能自动修正因为外部手动操作而导致的数据不一致问题。
LLM输出可靠性保障机制：针对LLM在生成靶场配置的时候可能会输出非法镜像名这个问题，本文给系统设置了两层防护。第一层是Prompt层，通过镜像白名单来做约束；第二层是代码层，会对LLM返回的配置做自动修补和校验。有了这个双重保障，最终配置的可用性就有了保证。
可观测性原则：后台独立运行了一个WatchdogService守护服务，会持续地去采集宿主机的CPU、内存、磁盘使用率以及Docker容器的状态，同时还会完整记录攻防操作的日志。这样就给系统的排障和性能分析提供了数据基础。
3.2系统总体架构
系统采用三层架构，从下到上依次是基础设施层、服务层和表示层。
3.2.1基础设施层
这层以Docker Engine为核心，负责容器的生命周期管理和网络隔离。像数据库、攻击机、目标主机这些靶场组件，都是以独立容器的形式在运行，然后通过Docker的bridge网络连接起来，这样既能实现组件之间的内网互通，又能做到对外隔离。
3.2.2服务层
服务层是由Flask应用服务器来承载业务逻辑的，包含了下面几个功能模块：
用户认证模块：这个模块基于JWT来实现无状态认证。用户登录成功之后，服务端会签发一个有效期为24小时的访问令牌，后续的请求就通过请求头来携带这个令牌完成身份验证。
靶场管理模块：封装了Docker SDK的调用，提供靶场的创建、启动、停止、删除这些操作，并且通过跟Docker Engine做实时状态同步，来保证前端展示的状态跟底层实际运行的状态是一致的。
多智能体模块：这个模块里面包含了EnvAgent、AttackAgent、DefenseAgent这三个专用的智能体，由Orchestrator编排器来做统一调度。它们分别负责靶场的自动创建、攻击执行以及防御响应，形成一个“创建→攻击→防御”的完整闭环。
LLM服务模块：封装了DeepSeek API的调用，提供模型路由、少样本注入、响应解析（从LLM的输出里面提取结构化JSON）等功能。
监控服务模块：这个模块以守护线程的形式在后台运行，通过psutil库周期性地去采集宿主机的CPU、内存、磁盘使用率以及Docker容器的运行状态。当指标超过预设阈值时就会触发告警并且记录日志。
异步任务模块：基于简单的生产者-消费者模式设计，目的是把攻击执行、靶场部署这些可能会阻塞主线程的任务给异步化。前端提交任务之后会立即返回一个任务ID，后台的工作线程独立去执行，结果就存到缓存里面供前端轮询获取，这样就避免了API接口长时间阻塞。
3.2.3表示层
表示层是基于Vue 3和Element Plus来构建的，实现了靶场列表管理、AI场景生成、攻击面板、防御告警、拓扑图可视化这些前端功能。前端通过Axios拦截器统一给请求附加上JWT令牌，并且用轮询机制定期地拉取攻击任务的执行进度。

图 3.1 系统架构图
3.3多智能体协同设计

图 3.2 多智能体协同流程图
Orchestrator编排器是三个Agent的协调中心，主要负责任务分发、状态管理还有错误恢复。假如某个Agent执行失败了，Orchestrator就会根据情况决定接下来是重试还是做降级处理。有了这一层就保证了局部出错的时候整体流程不会直接崩溃。
环境管理Agent负责靶场创建的整个过程。它接收用户的自然语言描述之后，会调用LLM来解析语义，生成标准化的JSON环境配置，然后再通过Docker SDK依次创建各个组件的容器，最后把容器信息写入数据库。另外，EnvAgent还维护着一个历史成功案例样本库，每次成功部署之后，它都会自动地把输入描述和输出配置存到训练样本里面去，后续生成任务中可选取相似案例作为Prompt示例。
攻击模拟Agent负责对靶场里面的目标执行模拟攻击。它会根据用户选择的攻击类型调用LLM来生成攻击步骤的规划，然后通过Docker exec在攻击容器里面执行相应的命令，最后把执行的结果实时地写入日志数据库。
防御分析Agent会监听攻击日志，调用LLM对攻击类型和影响进行分析，并生成防御建议，最后将结果写入defense_alerts表并展示到前端防御面板。
3.4数据库设计
系统用的是SQLite来做轻量级的持久化存储，主要的数据表有下面几张：
users表：存用户账号信息，包括用户名、密码哈希、角色、创建时间这些字段；
environments表：存靶场环境的元数据，比如名称、类型、状态、Docker容器名、配置JSON、创建时间等；
attack_logs表：存攻击执行的记录，包括目标环境ID、攻击类型、执行命令、结果摘要、时间戳这些内容；
defense_alerts表：存的是防御告警记录，关联了攻击日志ID，还有告警级别、处置建议、时间戳这些数据。
考虑到单机部署的场景要控制运维成本，所以数据存储选了SQLite，没有用更重的数据库。这样不需要额外启动一个数据库服务，既减少了一个潜在的故障点，也让整体的设计变得更轻量化了。

图 3.3 数据库E-R图


4系统关键模块实现
4.1LLM模块
在开发过程当中碰到的一个实际问题是：如果用同一套LLM调用参数去处理所有任务，出来的效果往往不太理想。拿具体的任务来说，自然语言理解这一块需要模型有一定的发挥空间，所以温度要设到中等；结构化JSON生成则要求输出高度确定，温度就得设得极低；攻击规划需要一些策略上的多样性，温度要偏高一些；而防御决策这部分要求保守稳定，温度就接近零。
针对不同任务对推理能力的差异化需求，系统采用的是Chat与Reasoner的双模型分工策略。Chat主要用在自然语言理解、靶场配置生成、报告生成这些需要快速、精确输出的任务上面；Reasoner则用在攻击分析、防御决策这些需要深度推理的任务上。这两种模型共用同一个API接口，通过task_type这个字段在路由层做自动分流，不需要人工指定。
系统实现了一个多任务模型路由器model_router，维护一张任务类型到模型参数的映射表，一共涵盖5种任务类型。
表 4.1 任务类型与模型参数映射表
任务类型	模型	温度参数	最大token数	设计依据
nlp_understanding	Chat	0.20	1000	温度低一点，语义解析更稳定
range_generation	Chat	0.10	2500	极低温度，保证JSON结构完整
attack_analysis	Reasoner	0.70	400	中高温度，方便多角度解读
defense_decision	Reasoner	0.05	800	接近零温度，保证防御判断确定性
scenario_expansion	Chat	0.65	1500	中高温度，用来生成场景变种
当EnvAgent需要把用户描述解析成结构化的配置时，它会依次调用nlp_understanding来解析意图，再调用range_generation生成JSON，通过这两个任务的配合保证最终输出的Docker配置精度是足够的。
每次路由调用的时候系统都会在日志里面记录下task_type、用的模型、温度参数还有最大token数是多少，格式就是“task_type→model+temperature+max_tokens”，这样后面要分析不同模型在各个任务上的实际表现就变得很方便。

图 4.1 任务路由流程图
4.2AI靶场生成与少样本学习
4.2.1镜像白名单与配置约束
有几点需要先说明，AI生成靶场这部分所部署的容器，全部来自经过验证的标准Docker镜像，比如nginx:alpine、mysql:8.0、ubuntu:22.04这些。它们都是干净的官方镜像，本身并不带任何预置漏洞。用户描述里面写的“包含SQL注入漏洞”这些属于场景语义标注，说的是这个靶场的训练用途，并不是容器里面真的装了一个带漏洞的Web应用。要是想部署DVWA、WebGoat这些真实含漏洞的靶场，那就需要对应的专用镜像，现在系统里面已经包含了三种这类的镜像。当前的设计重心主要还是放在容器的可部署性和平台稳定性上面，漏洞模拟主要是通过AttackAgent的攻击仿真层来呈现。
在开发初期测试的时候发现，光靠Prompt约束，LLM偶尔会输出一些不存在的镜像名。为了解决这个问题，设计在EnvAgent里面维护了一个经过实测验证的镜像白名单，每个镜像条目都包含了正确的标签、必需的环境变量（拿MySQL来说，就是MYSQL_ROOT_PASSWORD）、启动命令（比如ubuntu的sleep infinity）以及对外的暴露端口，以下是镜像白名单的代码示例：
VERIFIED_IMAGES = {
    'nginx:alpine':{'ports': [80],'env':{},'command': None},
    'mysql:8.0':{'ports':[3306],'env':{'MYSQL_ROOT_PASSWORD':'Range@123'}},
    'ubuntu:22.04':{'ports': [22],'env':{},'command': 'sleep infinity'},
    'redis:alpine':{'ports': [6379], 'env':{},'command': None},
    'node:18-alpine':{'ports': [3000], 'env':{'NODE_ENV': 'development'}},
    'python:3.11-slim':{'ports': [8000], 'env':{},'command': 'sleep infinity'},
}
系统把白名单以约束条件的形式写进Prompt里面传给LLM，同时代码层还有一个修补函数_patch_components()，会对LLM返回的配置做自动校验和补全——要是哪个字段缺失了，就从白名单里把默认值填进去。这种“Prompt约束+代码补丁”的双重保障机制，有效地降低了LLM输出非法配置的概率。
4.2.2少样本提示机制
为了提升LLM生成靶场配置时的稳定性，系统实现了一套轻量级少样本提示机制。Brown等人提出了情境学习的概念，证明大语言模型无需参数更新，凭提示词中的少量示例即可完成新任务[13]。本文借鉴这一思路，将历史成功部署的配置案例以"输入描述→输出JSON"对的形式注入提示词，引导模型生成格式更稳定的靶场配置，整个过程不涉及模型本身的修改，也不需要额外的训练计算资源，更准确地说是一种少样本提示增强方法。这套机制的逻辑分成下面三部分：
（1）样本积累。每次靶场成功部署之后，系统会自动把用户的原始描述和最终生成的完整配置，附上时间戳一起存到training_examples.json这个文件。文件会随着系统的使用自动累积。
（2）样本匹配。在处理新请求的时候，_find_relevant_examples()这个函数会对历史样本做关键词频率匹配打分，计算一下新请求跟每个历史样本描述之间的词汇重叠度，然后选取得分最高的1到3个样本作为参考案例。
（3）Prompt注入。把选中的相似样本，以“输入→输出”对的形式注入到系统提示里面，引导模型按照相似的结构和格式生成新的配置。
4.2.3容器创建与状态同步
EnvAgent在解析出靶场配置之后，会调用create_environment()这个方法依次为每个组件创建Docker容器。下面是几个实现细节：
（1）端口绑定：系统用的是0.0.0.0绑定，而不是127.0.0.1，这样可以确保容器服务能从宿主机的外部被访问到。
（2）环境变量传递：像MySQL的MYSQL_ROOT_PASSWORD这种必需的参数需要通过environment=传进去，不然容器启动之后马上就会退出来。
（3）前台进程保障：ubuntu、python这些镜像没有默认的前台进程，要是不加command='sleep infinity'，容器创建完就会直接停止。
（4）逐容器入库：每个组件的容器创建成功之后就立刻把它写入数据库，这样就能保证每个组件都能在靶场管理页面里面单独显示和管理。
（5）Docker优先的状态同步：在靶场列表查询部分，先通过docker.containers.list把宿主机上所有容器的真实状态拿到，然后再跟数据库里面的记录做对比，自动补充缺失的记录、删掉那些孤立的记录，这样就能保证前端展示的内容跟Docker的实际状态是一致的。
上面这些处理确保了容器创建过程的稳定性，也避免了因为参数缺失导致容器异常退出这些常见问题。
4.3攻防模拟实现
4.3.1攻击部分
Hutchins等提出的Cyber Kill Chain攻击生命周期模型[14]，给划分攻击阶段提供了理论框架。至于攻击场景的生成方法，本文结合了MITRE ATT&CK Enterprise的战术分类[15]来补充每个阶段的技术细节。Kill Chain原本有7个阶段，考虑到容器化仿真环境中，武器化阶段（制作载荷）和投递阶段（传递载荷）在命令执行层面难以明确区分，系统把阶段2跟阶段3合并成了一个仿真步骤，形成6阶段的压缩映射。
表 4.2 攻击6阶段表
阶段	名称	Kill Chain对应	MITRE ATT&CK战术	典型技术
1	侦察	Stage 1	TA0043、TA0007	端口扫描、服务识别、Web目录枚举
2	武器化与投递	Stage 2+3	TA0042、TA0001	漏洞扫描、攻击载荷准备
3	漏洞利用	Stage 4	TA0001、TA0002、TA0004	SQL注入、RCE、反序列化攻击
4	持久化与提权	Stage 5	TA0003、TA0004	后门植入、隐藏账户、权限提升
5	横向移动	Stage 6	TA0008、TA0011	内网扫描、凭证利用、横向访问
6	目标行动	Stage 7	TA0009、TA0010	敏感数据读取、数据外传、日志清理
为了在演练过程中体现不同攻击阶段的检测难度，系统定义了检测因子（Detection Factor,dk）。这个因子是本文根据攻击阶段可观测性的差异设计的模拟参数，跟攻击阶段是相关的，数值越高就说明这个阶段的行为越容易被检测到，反过来，数值越低就越隐蔽。该参数主要服务于系统演练效果的展示，不代表真实网络环境中的精确检测概率。
表 4.3 攻击阶段检测因子对应关系表
阶段	检测因子(dk)	说明
侦察	0.90	行为频繁，特征较明显
武器化与投递	0.82	攻击准备和投递行为具有一定可识别性
漏洞利用	0.72	恶意载荷触发后较容易形成异常记录
持久化与提权	0.55	隐蔽性增强，需要结合行为上下文判断
横向移动	0.38	行为更接近正常访问，检测难度较高
目标行动	0.25	动作更隐蔽，需要综合上下文分析
系统目前支持16类常见攻击场景，主要包括SQL注入、XSS、CSRF、文件包含、命令执行、SSRF、XXE、权限提升、容器逃逸、反弹Shell、端口扫描、暴力破解、中间人攻击、后门植入、横向移动和数据外传。这些攻击类型主要用于覆盖Web漏洞、系统层攻击、网络攻击和APT模拟等常见训练场景。这16类场景跟6阶段的Kill Chain形成了正交覆盖：前者描述的是“攻击是什么”，后者描述的是“攻击处在哪个生命周期阶段”。把这两个维度结合起来，就可以同时提供技术标注和阶段标注。

图 4.2 四类攻击覆盖关系图[ 攻击阶段×技术类别正交覆盖矩阵,深紫色(高覆盖)→浅紫色(中等覆盖)→浅粉色(低覆盖)。]
每次攻击任务都由AttackAgent来驱动，它先调用LLM（attack_planning任务，用deepseek-chat模型，温度设0.85）生成多步骤的攻击规划，然后再按步骤在靶场容器里面执行相应的命令，把每一步的命令跟返回结果实时地写到attack_logs表里面。前端通过轮询请求来获取最新的日志，这样就可以实现攻击过程的可视化。
4.3.2防御部分
防御响应模块由DefenseAgent负责。它会根据攻击日志识别攻击类型，并结合当前防御等级和已启用的防御规则，计算本次攻击的拦截结果和防御建议。本文参考NIST SP 800-61[16]的事件响应思想，把系统中的防御状态抽象为五个等级。但NIST SP 800-61主要提供事件响应流程参考，并没有直接给出五级防御等级。本文的五级防御状态是根据系统功能和教学演练需要设计的分级。
表 4.4 防御状态表
等级	名称	响应侧重点	系统动作
1	监控级	检测与记录	采集日志，记录攻击行为，不主动阻断
2	过滤级	基础过滤	对明显异常请求进行模拟过滤
3	阻断级	主动阻断	对可疑攻击行为进行模拟阻断
4	封禁级	隔离与限制	生成封禁建议，并限制相关攻击来源
5	应急级	综合处置	生成高强度防护建议和恢复建议
为了让防御效果能够被量化展示，本文设计了一个简化的防御拦截率模型。该模型不是对真实网络攻击拦截概率的精确预测，只是一个面向靶场教学演练的模拟。模型主要考虑防御等级、规则覆盖率、攻击阶段和攻击强度四个因素，防御等级表示系统当前整体防护能力；规则覆盖率表示当前启用规则对攻击类型的覆盖情况；攻击阶段关联攻击行为的可检测程度；攻击强度用于模拟攻击载荷增强后对防御效果的影响。
基础拦截率计算公式如下：
基础拦截率=(等级因子+覆盖率因子)×阶段因子×(1−0.015×攻击强度).....................................................................................................................................(4.1)
这里等级因子是随着防御等级1到5级而增加的，覆盖率因子由当前激活的防御规则的覆盖情况决定；阶段因子对应表 4.3中的检测因子，反映不同攻击阶段的可检测程度，侦察阶段最高，目标行动阶段最低；攻击强度取值1到10，强度越高对拦截率的削弱越明显。
在基础拦截率的基础上，系统会进一步叠加数据库里面已启用规则的加成。规则匹配采用的是名称关键词匹配的策略：只要规则的名称或者类型字段里面包含了当前攻击类型的关键词，就算匹配成功。每条匹配成功的规则，会根据它的覆盖率参数额外提供5%到15%的拦截率加成，所有规则加成的总上限是40%。最终的拦截率会被限制在5%到95%之间，计算公式是：
最终拦截率=min(0.95,max(0.05,基础拦截率+规则加成))....................................................................................................................................(4.2)
整体流程为：攻击执行完之后，DefenseAgent会自动触发，调用LLM（defense_decision任务，用deepseek-reasoner模型，温度设0.05）输出结构化的防御建议，里面包含了威胁分类、影响评估等。分析的结果会存到defense_alerts表里面并且实时地推送到前端的防御面板上。
针对防御动作的实际落地问题，本文在防御等级达到4级（封禁级）时引入了Docker容器网络隔离机制，对应模块实现于services/docker_isolate.py。上述拦截率模型给出的是防御效果的量化展示，而Docker网络隔离则是在此基础上执行了一次真实的网络层操作。从NIST SP 800-61事件响应框架的Eradicate（根除）阶段出发，封禁级防御的核心动作是隔离受损端点、切断攻击路径。系统通过调用Docker SDK的network.disconnect(container, force=True)接口，将靶场容器从其所在的bridge网络中真实断开，使其在演练场景内的网络通信立即中止，可通过docker inspect命令验证隔离效果。整个隔离操作的执行流程如下：
（1）容器定位：DefenseAgent在触发封禁级防御时，会将当前攻击的目标端口传入block_container_by_port()函数。该函数查询数据库中状态为running的靶场记录，通过匹配targets表配置JSON中的host_port或container_port字段找到对应容器，并进一步向Docker Engine确认容器实际处于运行状态，防止使用数据库残留记录造成误操作。
（2）网络断开：找到目标容器后，系统枚举该容器当前接入的所有网络，仅断开bridge类型的用户自定义网络，保留host和none模式以避免容器进程因失去网络而崩溃。对每个可断开的网络调用net.disconnect(container, force=True)，force参数确保即使容器正在处理请求也能立即生效。隔离结果写入全局_blocked字典，记录容器名称、被断开的网络列表、隔离时间戳、触发端口及攻击类型，同时在日志中输出INFO级别记录。
（3）自动恢复：在演练场景下，永久断网会影响后续训练流程，因此系统在隔离完成后会启动一个守护定时线程，60秒后自动调用unblock_container()将容器逐一重新加入隔离前的所有bridge网络。守护线程以daemon=True方式创建，不会阻塞主进程退出。用户也可以通过POST /api/defense/unblock/<name>接口在60秒内提前手动解封。_blocked字典的单例设计还确保了对同一容器的重复封禁请求不会触发二次操作。
（4）降级保护：如果Docker客户端初始化失败（如Docker Desktop未启动）或在数据库中找不到对应容器，block_container_by_port()会返回失败状态，DefenseAgent此时回退为文本记录（在action列表中写入IP封禁记录），演练流程不中断。这与整个系统"降级不崩溃"的设计原则保持一致。

图 4.3 防御响应流程图
4.3.3沙箱攻击执行模式
4.3.1节描述的攻击部分以LLM生成多步骤攻击规划为主，每一步的命令与输出均来自模型预设的模拟文本，不产生真实网络流量，这在演示攻击流程时能够满足基本的可视化需求，但无法产生真实可验证的攻击输出。为此，本文在AttackAgent中引入了沙箱攻击执行模式（services/sandbox_executor.py），提供一种可与LLM仿真模式按需切换的真实命令执行路径。
在设计上，沙箱模式采用sibling容器架构，即攻击容器与靶场容器共用同一宿主机上的Docker daemon，而非嵌套的Docker-in-Docker方案。这一选择使攻击容器能够加入靶场容器所在的bridge网络，通过Docker内网IP直接与目标通信，同时避免了Docker-in-Docker在权限管理和性能方面的额外开销。攻击容器使用python:3.11-slim镜像，该镜像已在系统白名单中（标准官方镜像，仅包含Python解释器），攻击脚本完全依靠Python标准库（socket、urllib）实现，无需在容器内安装任何第三方安全工具，保证了镜像来源的可控性。
每次沙箱攻击的完整执行流程分为以下几个步骤：
（1）目标定位：由get_target_network_and_ip()函数负责，输入用户指定的主机端口（host_port），查询数据库定位到对应的运行中容器，随后通过Docker SDK读取该容器的NetworkSettings，获取其在bridge网络中的内网IP以及容器内部服务端口（container_port）。沙箱容器在Docker内网中通信时，必须使用容器内部端口，而非经过NAT映射的主机端口，因此这一步需要区分两类端口的含义。若目标容器不存在或已停止，函数返回空值，后续步骤自动降级为LLM仿真模式。
（2）镜像检查：在创建攻击容器之前，系统先通过client.images.get(ATTACKER_IMAGE)检查python:3.11-slim镜像是否已在本地缓存。若镜像未拉取，直接返回提示信息并降级，而非触发自动拉取导致长时间等待。
（3）容器创建与执行：通过client.containers.run()启动攻击容器，关键参数包括：network指定为目标容器所在的bridge网络（使攻击容器加入同一内网）；mem_limit=64m和cpu_quota=25000（cpu_period=100000，即25%单核配额）限制资源占用，防止攻击脚本导致宿主机资源耗尽；remove=False、detach=True使容器后台运行并保留日志，待执行完成后手动清理。容器以python3 -c <script>方式执行攻击脚本，__TARGET__和__PORT__占位符在运行时替换为目标IP和端口。
（4）超时控制与清理：通过container.wait(timeout=20)等待容器结束，若20秒内未结束则依次调用container.kill()和container.remove(force=True)强制终止并删除容器。正常结束后，系统读取容器的stdout/stderr日志，并同样调用container.remove(force=True)清理容器，确保不在宿主机留下残留。
（5）输出解析与全失败检测：系统尝试将容器输出解析为JSON，若解析成功则进一步调用_check_all_probes_failed()检测是否所有探测均因连接被拒绝而失败。对于HTTP类攻击（SQL注入、XSS、SSRF等），若靶场容器没有运行HTTP服务，所有请求均会返回Connection refused错误，此时沙箱虽然成功执行但实际攻击无效，系统将此情况单独标注，避免将"Connection refused全集"误作有效攻击输出展示给用户。若JSON解析失败（如脚本运行时异常），则直接返回原始文本。
系统目前支持10类攻击场景的沙箱真实执行，如表4.X所示。其余6类攻击场景（CSRF、文件包含、XXE、权限提升、容器逃逸、反弹Shell）在容器化环境的命令执行层面难以用纯Python标准库复现，因此自动降级为LLM仿真模式，降级行为对用户透明。

表 4.X 沙箱执行支持的10类攻击场景
攻击类型	实现方式	探测目标	典型输出字段
端口扫描	socket.connect_ex多端口遍历	16个常见端口	open_ports、banner
服务识别	HTTP banner抓取+TCP握手	目标HTTP端口	http_status、server、tcp_banner
SQL注入	urllib探测多路径+关键词匹配	Web应用SQL错误响应	results（含sql_error_hint）
XSS攻击	反射型payload注入检测	HTML响应体	results（含reflected）
SSRF攻击	构造?url=探测内网/云元数据	目标Web SSRF参数	results（含reachable）
Web目录枚举	12条常见路径批量探测	HTTP状态码	found、404_count
暴力破解	Basic Auth凭证遍历	HTTP 200响应	hits
横向移动	172.17.0.0/24子网多端口扫描	Docker默认桥接网段	discovered
数据外传	API路径探测	敏感数据接口响应	probed、exposed
命令执行	?cmd=参数注入检测	RCE关键词响应体	results（含rce_hint）

前端攻击面板提供仿真模式与沙箱模式的切换卡片。沙箱模式激活时，攻击面板额外展示一个独立的"沙箱真实输出"卡片，以终端样式显示来自容器的JSON输出，并通过标签区分"真实执行"（sandbox_real=True）和"已降级"（sandbox_real=False）两种状态，使用户可以直观判断本次攻击是否产生了真实网络交互。

图 4.8 沙箱攻击模式前端展示
4.4后台WatchdogService监控服务
WatchdogService是系统后端的独立监控守护组件（services/watchdog.py），在Flask应用启动时由init_watchdog()函数初始化，以守护线程形式在后台持续运行，不占用主线程资源，也不影响API请求的正常响应。
4.4.1架构设计
WatchdogService采用的是"注册-巡检-告警"三阶段设计模式。

图 4.4 WatchdogService架构图
注册阶段：通过register_monitor()这个接口向服务注册监控项，每个监控项里面包含了名称、检查函数、巡检间隔、告警阈值以及告警描述。这种基于回调函数的注册机制让扩展变得很简单，想加一个新的监控指标只需要传入一个检查函数就行了，不需要修改核心的循环逻辑。
巡检阶段：守护线程在_monitor_loop()里面循环执行，它会遍历所有已经注册的监控项，调用各自的检查函数去获取当前的指标值，然后休眠指定的间隔再进入下一轮的巡检。
告警阶段：如果指标值超过了告警阈值，就会生成一条告警记录，写到内存告警队列里面，告警最多保留100条，滚动覆盖。同时通过Python的logging模块输出WARNING级别的日志。
表 4.5 监控项配置表
监控项名称	检查函数	巡检间隔	告警阈值	告警描述
cpu_usage	SystemMonitor.check_cpu_usage	30秒	80%	CPU使用率过高
memory_usage	SystemMonitor.check_memory_usage	30秒	85%	内存使用率过高
disk_usage	SystemMonitor.check_disk_usage	60秒	90%	磁盘使用率过高
docker_containers	SystemMonitor.check_docker_containers	30秒	无	仅记录容器数量
（无告警）
4.4.2系统资源监控
SystemMonitor类封装了系统资源指标的采集逻辑，主要使用psutil库获取CPU、内存和磁盘使用情况，并通过Docker SDK统计当前运行中的容器数量。CPU使用率通过固定采样间隔获取，告警阈值设置为80%；内存使用率采集物理内存占用比例，告警阈值设置为85%；磁盘使用率采集根分区占用比例，告警阈值设置为90%；Docker容器数量暂不设置告警阈值，主要用于观察系统中靶场容器数量的变化。
如果psutil库没有安装，各个检查函数会返回一个合理的默认值：CPU给25%，内存给50%，磁盘给40%，这样就能保证WatchdogService在依赖缺失的环境下仍然可以正常启动，不会影响系统的其他功能。
4.4.3与防御体系的协同
WatchdogService的系统资源监控与DefenseAgent的攻击行为分析形成互补关系。WatchdogService负责从主机视角持续采集CPU、内存、磁盘等资源指标，当检测到异常时触发告警；DefenseAgent从网络和行为视角分析攻击日志，识别具体攻击类型。当系统出现异常时，WatchdogService可以帮助判断异常是否与主机资源占用过高有关；DefenseAgent则可以帮助判断异常是否与攻击模拟行为有关。两类信息最终都可以在日志或前端页面中展示，为系统排查问题提供参考。
4.4.4告警数据对外接口
WatchdogService提供了下面几个对外接口，供后端的路由层调用之后，再通过API暴露给前端。
get_all_monitors()用于返回当前已注册监控项的状态，包括监控项名称、最后检查时间和最近一次指标值；get_alerts(limit=50)用于获取最近若干条告警记录，默认返回50条；clear_alerts()用于清空内存中的告警队列，一般只允许管理员操作。
4.5前端关键实现
前端基于Vue3和Element Plus实现，主要包括AI场景生成页、靶场管理页和攻击模拟面板等页面。前端通过Axios与后端API通信，并在需要身份校验的接口中携带JWT令牌。
4.5.1AI场景生成页
用户在自然语言的输入框里面提交了描述之后，前端会向/api/agents/ai-range/generate接口发起一个POST请求，等LLM解析完了返回配置预览；用户确认配置之后，再向/api/agents/ai-range/deploy发起部署请求。该页面采用分步交互方式，先展示AI生成的靶场名称、组件列表、镜像和端口等信息，再由用户确认是否部署。这样可以避免LLM生成结果直接进入部署流程，也方便用户在部署前发现一些可能存在的明显错误。

图 4.5 AI场景生成页
4.5.2靶场管理页
靶场管理页用于展示当前系统中已经创建的靶场环境。页面会从后端获取容器列表和运行状态，并展示容器名称、镜像、端口、状态和创建时间等信息。靶场状态以Docker Engine返回的实际运行状态为准，前端展示的数据来自后端同步后的结果。用户可以在该页面执行启动、停止和删除等基础管理操作。

图 4.6 靶场管理页
4.5.3攻击面板
攻击面板用于发起和查看攻击模拟任务。在攻击任务执行期间，以每2秒一次的频率轮询拉取任务进度和最新的攻击日志；任务结束之后，会自动清除定时器停止轮询。攻击日志会展示攻击命令和返回结果，帮助用户观察攻击模拟过程。

图 4.7 攻击模拟面板

5测试与评估
5.1测试环境
本系统的测试在以下环境中进行：
宿主机：Windows 11，Intel Core i7-12700H，16GB内存

Docker：Docker Desktop 4.x，Docker Engine API v1.47
后端：Python 3.8，Flask 3.x，Flask-JWT-Extended 4.x
前端：Node.js 20.x，Vue 3.4，Vite 5.x，Element Plus 2.x
LLM：DeepSeek API
5.2功能测试
5.2.1
5.2.1靶场自动生成测试
针对AI靶场生成这个模块选取了6类典型的靶场模板来做测试，覆盖了Web服务、数据库服务、漏洞靶场、容器安全等场景。测试的时候重点关注三个指标：配置解析的成功率、容器启动的成功率以及场景的匹配度。

表 5.1 靶场自动生成测试结果表
场景名称	组件数	配置生成(s)	容器部署(s)	总耗时(s)
Web安全靶场（nginx+mysql）	2	3.25	2.10	5.35
数据库服务靶场（mysql+redis）	2	3.48	2.13	5.61
DVWA综合漏洞靶场	1	6.01	2.08	8.10
WebGoat OWASP教学靶场	2	3.84	2.10	5.94
容器安全靶场	2	4.12	2.15	6.27

测试的结果表明，在引入了镜像白名单约束和代码级补丁这双重保障之后，所有的测试用例都能够成功地从自然语言解析一直走到容器部署的全流程。调用DeepSeek API生成配置这一步是主要的时间开销，大概占了60%到75%。
下面以“创建一个包含nginx和mysql的Web环境”这个输入作为例子，来演示系统里面AI靶场生成的全流程。

图 5.1 AI靶场生成流程展示图
如图 5.1所示，在系统的AI靶场页面输入场景描述之后，LLM解析自然语言生成靶场名称、组件等相关配置，用户可在第二步增删漏洞类型，进行靶场配置修改，点击确认后页面显示部署进度条与部署结果。部署完成后，如图 5.2，相关容器会出现在系统靶场列表与Docker Desktop容器列表中。

图 5.2 AI靶场真实结果图
5.2.2攻防演练测试
针对SQL注入、XSS、端口扫描、暴力破解等攻击场景，最后进行了功能验证，确认系统能够正确执行攻击命令序列并记录日志。

图 5.3 攻防演练展示图
5.2.3WatchdogService测试
对WatchdogService的功能验证，主要采用了下面这几种方式：
（1）调用get_alerts()这个接口，确认告警记录里面包含了正确的字段；
（2）往告警队列里面注入101条测试告警，验证当超过100条上限之后，旧的告警会自动被滚动清理掉，并且50条告警所占的内存只有12KB左右；
（3）模拟CPU监控值超过阈值的情况，验证_watchdog这个方法能够正确地生成告警并且写入队列；
（4）先把WatchdogService停掉然后调用clear_alerts()，确认告警队列能被正确地清空。
WatchdogService的监控项配置以及告警阈值的验证结果，见表 5.2。从验证结果来看，四类监控项的告警逻辑都通过了验证：CPU和内存超过阈值的时候，能够正确地触发告警；磁盘低于阈值的时候不会触发；Docker容器数没有设阈值，所以只做记录。


表 5.2 WatchdogService监控告警测试结果表
监控项	检查函数	告警阈值	测试值	是否触发告警	验证结果
CPU使用率	check_cpu_usage()	80%	90%	触发	通过
内存使用率	check_memory_usage()	85%	87%	触发	通过
磁盘使用率	check_disk_usage()	90%	75%	未触发	通过
Docker容器数	check_docker_containers()	无阈值	12	仅记录	通过
5.3性能评估
5.3.1靶场生成时间
对含不同数量组件的靶场进行了生成时间测量，结果如表 5.3所示。总耗时为从用户提交描述到所有容器Running状态。
表 5.3 靶场生成时间测试结果表
场景	组件数	配置生成(s)	容器部署(s)	总耗时(s)
Web服务	2	3.25	2.10	5.35
数据库服务	2	3.48	2.13	5.61
DVWA漏洞场景	1	6.01	2.08	8.10
WebGoat漏洞场景	2	3.84	2.10	5.94
JuiceShop漏洞场景	1	6.20	2.07	8.26
平均	-	4.56	2.10	6.65
容器创建时间主要取决于镜像是否已在本地缓存。上表数据基于镜像已拉取至本地的情况；若需要首次拉取，时间将增加。Docker镜像源偶尔会超时，这类工程细节在实际部署时难以完全规避，但系统目前支持的几类镜像源皆已经预先拉取镜像，故能实现在较短时间内创建靶场。

图 5.4 不同类型靶场缓存镜像前后生成时间对比柱状图
5.3.2API响应时间
对主要API接口进行平均响应时间测量，结果如表 5.4所示。
表 5.4 主要API接口响应时间测试结果表
接口名称	接口路径	平均响应(ms)	最快(ms)	最慢(ms)
Agent状态查询	/api/agents/status	2049.19	2036.37	2057.67
Dashboard统计	/api/stats/overview	2059.00	2051.39	2076.74
AI靶场模板列表	/api/agents/ai-range/scenarios	2053.00	2044.00	2069.00
靶场列表查询	/api/env/list	2053.00	2043.42	2063.61
从测试结果看，主要接口响应时间稳定在约2秒左右。由于本文测试环境中部分接口包含状态同步、后台轮询或等待逻辑，因此响应时间存在固定开销。后续可以进一步优化接口异步处理和状态缓存机制，以降低前端等待时间。

图 5.5 部分接口平均响应时间与P95响应时间柱状图
5.3.3少样本学习效果
为了验证少样本学习机制到底有没有效果，设计在不同的样本数量下，对靶场配置解析任务做了对比评估，测试时主要观察生成的组件类型是否与用户描述匹配，以及生成结果是否能够通过后续的镜像白名单校验和字段补全流程。

图 5.6 少样本学习效果测试折线图
从测试结果可以看到，在没有注入历史案例时，LLM生成配置时更容易出现组件遗漏或字段不完整的问题。随着上下文示例数量增加，配置生成的准确率有一定提升。在本文测试条件下，5-shot设置下的表现较好；但当示例数量继续增加到10-shot时，准确率反而下降。这说明示例数量并不是越多越好，过多示例可能会增加Prompt长度，也可能干扰模型对当前用户需求的判断。系统实际运行时默认选取1到3个相似历史案例作为Prompt示例；5-shot和10-shot主要用于实验对比。综合测试结果和系统运行成本，本文最终采用少量相似案例注入的方式，而不是在每次生成时加入过多历史样本。另外，AI生成配置的最终可用性不只依赖LLM本身，还依赖JSON解析、镜像白名单校验和代码层字段补全等后处理机制。因此，少样本提示机制主要用于提升初始生成质量，后处理机制则用于进一步保证配置能够被系统部署。
5.4局限性分析
在开发和测试的过程当中，也发现了一些问题，主要体现在下面这些方面。
LLM输出稳定性限制：虽然系统通过Prompt约束、JSON提取、字段校验和重试机制提高了配置生成的成功率，但LLM输出仍然存在一定不确定性。在复杂需求下，模型可能生成格式不完整、字段缺失或组件选择不准确的配置。针对这种情况，系统目前采用正则提取JSON、字段校验、降低温度重试和默认模板降级等方式进行处理。但从根本上看，LLM输出稳定性仍然是系统后续需要继续优化的问题。
镜像范围限制：系统目前依赖镜像白名单来保证部署稳定性，白名单机制可以减少非法镜像和错误启动参数带来的问题，但也限制了系统支持的场景范围。当前系统支持的镜像主要包括基础服务镜像和少量漏洞靶场镜像。后续如果要支持更多真实漏洞复现环境，还需要持续维护镜像配置，包括端口、环境变量、启动命令和依赖服务等信息。
单机部署限制：系统当前基于单机Docker Engine运行，靶场规模和并发能力受到宿主机CPU、内存和磁盘资源限制。当同时运行多个靶场环境时，内存和端口资源可能成为瓶颈。虽然WatchdogService能够对资源使用情况进行告警，但它只能提供监控和提醒，不能从根本上解决资源调度问题。后续可以考虑引入Kubernetes等容器编排技术，以支持多节点部署和更大规模的并发演练。
评估规模限制：本文测试主要围绕功能可用性和基础性能展开，测试场景数量和样本规模有限。少样本提示效果、攻击模拟效果和防御建议质量仍需要在更大规模的场景中进一步验证。因此本系统更适合作为轻量级教学演示和基础攻防训练平台，而不是直接用于真实生产环境中的安全防护。


6总结与展望
6.1研究总结
本文围绕传统网络安全靶场搭建周期长、场景更新不够灵活、依赖人工配置等问题，设计并实现了一套基于多智能体与大语言模型的靶场自动生成系统。系统采用Flask+Vue前后端分离架构，以Docker作为底层运行环境，实现了自然语言需求解析、靶场配置生成、容器部署、攻击模拟、防御建议生成和资源监控等功能。本文主要完成了以下三方面工作。
6.1.1多智能体协同的靶场自动生成架构
本文设计了EnvAgent、AttackAgent和DefenseAgent三个功能型智能体，并通过Orchestrator编排器进行统一调度。EnvAgent负责自然语言解析和靶场环境创建，AttackAgent负责攻击步骤生成和模拟执行，DefenseAgent负责攻击日志分析和防御建议生成。通过这种方式，系统将靶场生成、攻击模拟和防御分析串联起来，形成了一个较完整的演练流程。
6.1.2多任务模型路由与少样本提示机制
基于DeepSeek大语言模型构建了一个多任务路由器，针对语义解析、配置生成、攻击分析、防御决策等不同任务，它会动态分配Chat或Reasoner模型，并配置差异化的温度参数。同时，系统引入历史成功案例作为少样本提示示例，在生成新配置时注入相似案例，以提高输出结构的稳定性。该方法不涉及模型参数训练，实现成本较低，适合本文系统的轻量级应用场景。
6.1.3容器监控与配置可靠性保障
为了提高AI生成配置的可部署性，系统采用镜像白名单、Prompt约束和代码层字段补全机制，对LLM输出结果进行校验和修正。针对系统运行状态，本文实现了WatchdogService后台监控服务，对CPU、内存、磁盘和容器数量进行周期性检查，并在资源超限时生成告警。通过这些设计，系统在单机部署条件下具备了基本的稳定性和可观测性。
总体来看，本文系统能够在镜像已缓存的条件下较快完成靶场部署，并支持多类常见攻击场景的模拟演练。但系统仍存在LLM输出不稳定、镜像范围有限、漏洞真实性不足和单机资源受限等问题，后续仍需要进一步完善。
6.2未来工作
6.2.1Kubernetes多节点扩展
当前系统是基于单机Docker Engine来部署的，靶场的规模和并发能力都受到宿主机CPU、内存这些资源的硬性限制。拿Kubernetes来说，它有自动调度、弹性伸缩以及服务发现这些机制，系统可以根据负载情况，动态地把靶场容器分配到不同的工作节点上从而支持更大规模的并发演练。同时，Kubernetes的声明式配置和自愈能力，也能提升系统的运维自动化水平以及容错能力。未来会考虑引入Kubernetes这个容器编排平台，用它替代单机Docker实现靶场的跨节点分布式部署。
6.2.2强化学习驱动的攻防对抗
当前系统中的AttackAgent和DefenseAgent主要采用规则编排与LLM生成相结合的方式完成攻击模拟和防御分析，还不具备真正的自主学习能力。后续可以考虑引入强化学习方法，使攻击智能体和防御智能体在多轮演练中根据环境反馈不断调整策略。Morris等人在CybORG环境中对CAGE Challenge训练得到的强化学习网络防御智能体进行了评估，并提出结合奖励值和面向网络防御的有效性指标来分析智能体表现[17]。说明如果系统未来要扩展为自适应攻防演练平台就需要在攻击日志、防御告警和环境状态的基础上进一步设计状态空间、动作空间、奖励函数以及评估指标。
6.2.3知识图谱增强的场景生成。
当前系统是基于用户的自然语言描述以及少样本学习机制来生成靶场配置的，场景的漏洞类型和攻击向量都是由LLM根据训练数据隐式地推断出来的，还是缺少跟真实漏洞库之间的显式关联。未来考虑引入CVE和NVD这两个漏洞知识图谱，建立起漏洞编号、受影响的软件版本、攻击向量以及靶场组件之间的结构化映射关系。这样一来系统就可以根据用户指定的CVE编号自动去检索对应的漏洞信息，然后生成精确复现该漏洞的靶场配置，从而提升训练场景跟真实威胁之间的对应精度，整个系统的真实度就会更大一步提升。
参考文献
[1]贾焰,方滨兴,李爱平,顾钊铨．基于人工智能的网络空间安全防御战略研究[J]．中国工程科学,2021,23(3):98-105．
[2]奇安信威胁情报中心．网络安全威胁2024年度报告[R]．北京:奇安信科技集团股份有限公司,2025．
[3]Zhang A K,Perry N,Dulepet R,et al．Cybench: a framework for evaluating cybersecurity capabilities and risks of language models[A]．The Thirteenth International Conference on Learning Representations[C]．Singapore:ICLR,2025．
[4]Yamin M M,Katt B,Gkioulos V．Cyber ranges and security testbeds: scenarios, functions, tools and architecture[J]．Computers & Security,2020,88:101636．
[5]蔡晶晶．人工智能大模型驱动网络靶场智能化演进[J]．计算机工程与应用,2024,60(3):189-196．
[6]Ukyo R, Nishida T, Yuge S, et al. CyExec*: A high-performance container-based cyber range with scenario randomization[J]. IEEE Access, 2021, 9: 109225-109236.
[7]付志远,田浩,张家睿,等．大语言模型的安全挑战与机遇[J]．信息安全学报,2024,9(5):26-42．
[8]Shen X,Chen Z,Backes M,et al．PentestAgent: incorporating LLM agents to automated penetration testing[A]．Proceedings of the 20th ACM Asia Conference on Computer and Communications Security[C]．New York:ACM,2025．
[9]江颉,王豪,李明达,等.基于大语言模型的多智能体自动渗透测试框架构建与评估[J].自动化学报,2026,52(04):821-832.DOI:10.16383/j.aas.c250293.
[10]Guo D,Yang D,Zhang H,et al．DeepSeek-R1 incentivizes reasoning in LLMs through reinforcement learning[J]．Nature,2025,645:633-638．
[11]梁建辉,侯昱辉,刘润福．基于Docker的网络安全靶场设计与实现[J]．铁路计算机应用,2022,31(11):70-74．
[12]唐俊晓．基于Docker的面向网络空间的漏洞靶场设计与实现[J]．网络安全技术与应用,2021(3):16-18．
[13]Brown T B,Mann B,Ryder N,et al．Language models are few-shot learners[A]．Advances in Neural Information Processing Systems[C]．Red Hook:Curran Associates,2020: 1877-1901．
[14]Hutchins E M,Cloppert M J,Amin R M．Intelligence-driven computer network defense informed by analysis of adversary campaigns and intrusion kill chains[A]．Proceedings of the 6th International Conference on Information Warfare and Security[C]．Reading:Academic Conferences International,2011．
[15]MITRE. MITRE ATT&CK matrix for enterprise[EB/OL]. https://attack.mitre.org/matrices/enterprise/. 2026-05-19
[16]National Institute of Standards and Technology. Computer security incident handling guide (SP 800-61 Rev.2)[R]. Gaithersburg: NIST, 2012.
[17]Morris A, Procter R, Wallbank C. Evaluating reinforcement learning agents for autonomous cyber defence[J]. Applied AI Letters, 2025, 6(3):e125. DOI:10.1002/ail2.125.