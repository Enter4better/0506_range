# 基于多智能体与大语言模型的网络安全攻防靶场自动生成系统的设计与实现

**摘要：** 传统网络安全靶场长期依赖人工搭建，建设周期动辄以周计，场景一旦固化就很难更新，维护成本也居高不下，面对快速演变的网络威胁，这类平台愈发力不从心。本文描述了一套我们实际设计并跑通的靶场自动生成系统——以 Flask + Vue 3 前后端分离为骨架，Docker 容器承担底层基础设施，三个专用 AI 智能体（环境管理、攻击模拟、防御分析）在 Orchestrator 编排器的统一调度下协同工作，整个推理链路由大语言模型（LLM）驱动。用户用一句自然语言描述想要什么样的靶场，系统就接管后续：解析语义、编排容器、部署漏洞环境、跑完攻防演练、输出结果报告，全程不需要人在旁边盯着。后端还跑着一个 WatchdogService 守护线程，持续巡检宿主机的 CPU、内存、磁盘和容器状态，指标越线立刻告警。测试结果显示，含 3～5 个容器的靶场从提交描述到全部 Running 基本在 2 分钟内完成；攻击侧覆盖 SQL 注入、XSS、CSRF、文件包含、命令执行、SSRF、XXE 注入、权限提升、容器逃逸、端口扫描、横向移动、数据外传等 16 类常见威胁。对于需要低成本、可快速复用的网络安全人才培训场景，这套系统提供了一个可行的自动化路径。

**关键词：** 网络安全靶场；多智能体系统；大语言模型；Docker 容器化；自动化攻防

---

## 第一章 绪论

### 1.1 研究背景

网络安全问题近年来已经从技术圈的小众话题变成了国家层面的核心议题。国家互联网应急中心在《2024 年中国互联网网络安全报告》中给出了一组让人印象深刻的数字——全球每天新增安全事件超过百万起，而攻击手法也早已从早期的端口扫描和口令爆破，演变为以 APT（高级持续性威胁）为代表的多阶段、多向量组合攻击 [1]。中国网络安全产业联盟 2026 年度报告则进一步指出，面向关键信息基础设施的定向攻击在近两年间增长超过 40%，技术门槛还在持续走低，安全事件的高频发生正在成为常态 [20]。在这种背景下，仅靠课堂上的理论讲解培养出来的安全从业者，在面对真实攻防场景时往往力不从心——实战技能的培养必须依托高仿真的演练平台 [12]。

网络安全靶场（Cyber Range）本质上是一套模拟真实攻防对抗环境的训练基础设施，能让学员在"零风险"的沙盒空间里反复练手 [29]。然而在实际建设中，传统靶场暴露出了几个反复出现的问题：搭建周期极长，通常需要数周甚至数月的人工配置；场景高度固化，部署完成后很难跟上新型漏洞和攻击技术的演变节奏；虚拟机镜像、网络拓扑、服务配置都需要专业人员手动维护，运维成本高；而每次为不同培训目标定制场景时，大量工作又得从头开始 [6][17]。

大语言模型（Large Language Model，LLM）的出现为解决上述问题打开了新思路。以 DeepSeek、GPT-4o 为代表的新一代 LLM 在自然语言理解、代码生成和逻辑推理方面展现出了相当强的能力，为自动化场景生成提供了技术基础 [4][8]。与此同时，Docker 容器化技术已经足够成熟，能够在秒级完成轻量级虚拟环境的创建与销毁，大幅降低了靶场基础设施的运维复杂度 [6]。把 LLM 的语义理解能力和 Docker 的快速部署能力结合起来，构建一套"按需自动生成"靶场的智能系统，在技术上是可行的，在工程上也有足够的落地价值。

### 1.2 研究意义

本研究的意义可以从三个层面来看。

**工程层面**，引入多智能体协同机制后，靶场生命周期的各个阶段——需求解析、环境编排、攻击模拟、防御分析、效果评估——分别由专用 AI 智能体负责，整个流程实现了自动化，人工搭建靶场的时间从以往的数天被压缩到了分钟级。ScenarioForge 等同类工作已经证明 LLM 多智能体协作在网络安全场景生成上的可行性 [15]，本研究在此基础上进一步把生成结果与 Docker 真实部署打通，跑完了从"描述需求"到"容器运行"的完整链路。

**训练层面**，用自然语言描述就能生成个性化靶场，意味着渗透测试工程师、安全运营人员、应急响应团队可以各自获得与实际工作场景高度对应的训练环境，而不是所有人都在同一套固定靶场里做练习。AI 驱动靶场的场景自适应生成能力，已被学界视为下一代靶场的核心特征 [26]。

**研究层面**，本系统探索了 LLM 在网络安全领域的落地路径，尤其是将少样本学习（Few-shot Learning）机制引入靶场配置生成——系统能从历史成功案例中持续学习，逐步提升语义解析精度 [24]。基于关键词频率匹配的轻量级方案，也为在资源受限条件下增强 LLM 能力提供了一种可参考的实现方式 [22]。

### 1.3 研究目标与内容

本研究的目标是设计并实现一个功能完整、可实际运行的 AI 驱动网络安全攻防靶场自动生成系统，具体研究内容包括三个方面：

**（1）多智能体协同的靶场自动生成架构。** 设计并实现三个专用 AI 智能体——环境管理 Agent（EnvAgent）、攻击模拟 Agent（AttackAgent）、防御分析 Agent（DefenseAgent），由 Orchestrator 编排器统一调度，通过 KillChain 流程串联攻防全流程。EnvAgent 负责根据用户需求自动生成靶场配置并完成 Docker 容器部署；AttackAgent 依据场景类型从预设攻击链库中选取攻击手段，对靶场实施模拟攻击；DefenseAgent 结合 LLM 推理与规则匹配生成防御建议并推送告警。

**（2）基于 DeepSeek LLM 的自适应模型路由与少样本学习机制。** 设计多任务模型路由器（ModelRouter），根据任务类型将请求分发至 DeepSeek-Chat 或 DeepSeek-Reasoner 模型，并动态配置 temperature、max_tokens 等推理参数，兼顾生成质量与响应速度。同时实现基于关键词频率匹配的少样本学习机制：系统自动将成功部署的配置以"输入→输出"对的形式存入训练样本库（training_examples.json），新请求通过词汇重叠度打分选取最相关的 1～2 个历史样本注入 Prompt，引导模型生成符合已有规范的靶场配置。

**（3）容器级安全监控与双重保障的靶场配置生成机制。** 设计并实现 WatchdogService 后台守护服务，对宿主机 CPU、内存、磁盘及 Docker 容器状态进行持续监控，异常时写入系统日志并触发防御告警。靶场配置生成阶段引入镜像白名单（VERIFIED_IMAGES）与代码级补丁（`_patch_components`）双重保障：白名单限制只能使用经过验证的镜像，补丁函数对 LLM 生成的配置进行格式化修正，确保 Docker API 调用的可靠性。

### 1.4 论文结构

本文共分六章：第一章介绍研究背景、意义与目标；第二章对相关技术和研究现状进行综述；第三章详细阐述系统总体设计方案；第四章描述各模块的具体实现过程，包括多智能体架构、模型路由与少样本学习机制、容器监控与配置保障机制；第五章展示测试结果与性能评估；第六章总结全文并展望未来研究方向。

---

## 第二章 相关工作与技术综述

### 2.1 网络安全靶场研究现状

靶场技术走到今天，大致经历了三代演变。最早是物理机靶场，真实性高，但成本极其高昂，大多数机构根本负担不起；之后虚拟化靶场以 VMware、KVM 为代表登场，靠虚拟机快照实现场景复现，但启动慢、资源占用大的问题一直没有根本解决；再往后就是容器化方案，Docker 和 Kubernetes 提供了更轻量的环境隔离，成为近几年学术界和工业界的主流选择 [6]。

Yamin 等人（2020）系统梳理了网络安全靶场的技术框架，将其归为物理、虚拟和混合三类，并明确指出动态场景生成是未来发展的关键方向 [29]。2024 年前后，生成式 AI 开始被引入靶场场景设计。Li 等人（2024）提出了基于提示工程的漏洞场景自动构造方法，验证了 LLM 能够根据 CVE 描述自动生成对应的 Docker 靶场配置，准确率达到 73%，但对 LLM 输出可靠性的约束还不够完善 [3]。Chen 等人（2025）进一步提出 RangeGPT 框架，将 LLM 与 Kubernetes 结合，实现了靶场全生命周期管理，在动态扩缩容方面的验证结果比较有说服力 [17]。本研究在此基础上引入了镜像白名单（VERIFIED_IMAGES）和代码级补丁（`_patch_components`）双重保障，把配置可用率拉到了接近 100%。

CISA 在其 2024 年国家网络靶场框架报告中指出，现代靶场必须具备三个核心属性：动态性（场景可按需生成）、真实性（漏洞与真实 CVE 对应）、可测量性（攻防效果可量化评估）[5]。本系统在三个维度都有对应设计：LLM 驱动的动态场景生成、基于 OWASP Top 10 的漏洞体系、攻击成功率与防御响应时间的量化记录。张浩等人（2026）将这类系统归纳为"第四代靶场"，认为其核心价值在于将人工运维成本降低 80% 以上 [26]。

### 2.2 大语言模型在网络安全领域的应用

LLM 在安全领域的渗透速度相当快。从最初的威胁情报文本提取，到漏洞扫描自动化，再到完整的自主攻击规划，能力边界一直在被拓宽 [19]。

Xu 等人（2024）构建了自主渗透测试系统 AutoPentest，无需人工干预就能完成多阶段渗透测试，在标准靶机上的任务完成率达到 63% [2]。Fang 等人（2025）的结论更让人注意——LLM 智能体在已知某漏洞存在的前提下，能够独立完成 87% 的 One-Day 漏洞利用，这个数字在安全社区引发了相当多的讨论 [13]。这些工作共同说明，LLM 执行复杂安全任务的基础能力是实打实存在的，但工具调用的稳定性和输出格式的规范性还有较大改善余地 [11]。

DeepSeek 系列模型在代码理解和结构化数据生成上的表现值得关注。DeepSeek-V3（2024）在代码基准测试中的得分已经达到甚至超越 GPT-4o 的水平，而 API 调用成本明显更低，这让它成为靶场生成系统核心推理引擎的合适选择 [4]。DeepSeek-R1（2025）则通过强化学习训练进一步强化了推理能力，在复杂逻辑推理任务上展现出类似"思维链"的行为模式 [9]。本研究正是基于 DeepSeek 的 Chat 模型构建 LLM 服务层，并根据不同任务类型动态调整推理参数。

Li 等人（2026）从攻击者视角出发，验证了 LLM 在辅助漏洞利用代码生成方面的有效性，同时强调必须建立完善的输出约束机制以防止误用 [27]。这与本研究在 EnvAgent 中引入镜像白名单和多层过滤的出发点是一致的。

### 2.3 多智能体系统设计

多智能体系统（Multi-Agent System，MAS）的思路是将复杂任务拆解给多个自主智能体，让它们通过协作或竞争共同完成目标。基于 LLM 的多智能体框架（如 AutoGen、CrewAI）近年来在软件工程、数据分析、金融等多个领域都有落地案例 [14]。

在网络安全场景下，多智能体的一个直观优势是可以把攻防双方建模为相互对立的智能体，模拟真实对抗过程。Wang 等人（2025）提出的 CyberAgent 框架让攻击者 Agent 和防御者 Agent 在同一环境中交替行动，通过博弈推演持续优化双方策略 [10]。Liang 等人（2026）则在此基础上构建了 MultiAgent-CTF 基准测试集，专门评估 LLM 智能体在 CTF 竞赛场景中的协作能力，给多智能体安全系统的标准化评估提供了参考框架 [21]。

本系统将环境管理、攻击、防御分别抽象为独立 Agent，但在工程落地上做了一个关键选择：采用规则驱动的 Orchestrator 编排器，而非完全自主的 Agent 间通信。这样做的原因很实际——纯 LLM 驱动的多智能体协议在调试时相当麻烦，行为难以预测。刘伟等人（2025）的研究也指出，工程化落地阶段引入规则约束层往往比纯自主协作更可靠 [14]，这与我们的实际经验一致。

### 2.4 容器化与系统监控技术

Docker 容器因为启动快、环境一致、资源隔离清晰，已经成为构建轻量级靶场环境的默认方案 [6]。Docker SDK for Python 提供了完整的编程接口，容器的创建、启停、网络配置、日志读取都可以通过代码控制，这为 AI 智能体自动管理靶场容器提供了必要的底层支撑。胡志鹏等人（2025）验证了基于容器编排的靶场弹性扩展方案，单次扩缩容平均耗时低于 15 秒 [18]，这个数字说明容器化方案在响应速度上确实有优势。

监控方面，守护进程（Watchdog）模式是一种经过验证的成熟设计——独立的守护线程周期性检查目标指标，超过阈值就告警。Park 等人（2026）提出的 ContainerGuard 系统将行为基线分析引入容器监控，能够在资源维度和行为维度同时感知容器健康状态 [23]。psutil 库提供跨平台的系统指标采集能力，与 Watchdog 模式配合使用可以构建出相当轻量的健康监控服务，是 Python 生态中这类需求的主流选择。

---

## 第三章 系统总体设计

### 3.1 设计原则

在动手实现之前，我们花了一些时间梳理设计原则，这些原则后来在遇到具体工程问题时都发挥了作用。

**职责分离原则**：前端只管交互与展示，后端只管业务逻辑，LLM 只管语义理解与内容生成，Docker 只管基础设施，各层之间通过明确定义的接口通信，不让任何一层承担不属于它的职责，也避免强耦合带来的联动故障。

**Docker 为真相单一来源原则**：系统将 Docker 的实际运行状态作为靶场状态的权威来源，数据库只作为元数据缓存使用。每次查询靶场列表时，系统先从 Docker API 拿到真实容器状态，再与数据库记录对比同步，确保前端看到的和底层实际跑的始终一致。这个原则解决了一个实际问题：如果有人在系统外手动操作了容器，数据库记录就会和实际状态不符，用 Docker 优先策略可以自动修正这种偏差。

**防御纵深原则**：LLM 在生成靶场配置时偶尔会输出不存在的镜像名，这不是假设风险而是实测中真实遇到的问题。为此，我们在 Prompt 层加了镜像白名单约束，在代码层加了自动修补逻辑，两道防线都挡住了，配置才算可用 [3]。

**可观测性原则**：后台独立运行 WatchdogService 守护服务，持续采集宿主机和容器资源指标，同时完整记录攻防操作日志。没有这些数据，排查问题时基本上是盲人摸象 [23]。

### 3.2 系统总体架构

系统采用三层架构，从下到上是基础设施层、服务层和表示层，各层之间通过标准化接口交互。

**基础设施层**以 Docker Engine 为核心，负责容器的生命周期管理和网络隔离。靶场中的每个组件（Web 服务器、数据库、攻击机、目标主机等）都以独立容器的形式运行，通过 Docker bridge 网络连接，实现组件间的内网互通和对外隔离。

**服务层**由 Flask 应用服务器承载所有业务逻辑，包含以下功能模块：
- **用户认证模块**：基于 JWT（JSON Web Token）实现无状态认证，访问令牌有效期为 24 小时；
- **靶场管理模块**（`compat.py`）：封装 Docker SDK 调用，提供靶场的增删查改与状态同步功能；
- **多智能体模块**（`agents/`）：包含 EnvAgent、AttackAgent、DefenseAgent 三个专用智能体和 Orchestrator 编排器；
- **LLM 服务模块**（`llm/`）：封装 DeepSeek API 调用，提供模型路由、少样本注入、响应解析等功能；
- **监控服务模块**（`services/watchdog.py`）：后台守护线程，周期性巡检系统资源与容器状态；
- **异步队列模块**（`services/async_queue.py`）：处理耗时较长的靶场生成和攻击任务，避免阻塞主线程。

**表示层**基于 Vue 3 + Element Plus 构建单页应用（SPA），实现靶场列表管理、AI 场景生成、攻击面板、防御告警、拓扑图可视化等前端功能。前端通过 Axios 拦截器统一为请求附加 JWT 令牌，并用 `setInterval`（每 2000 ms）轮询拉取攻击任务执行进度。

### 3.3 多智能体协同设计

三个 AI 智能体的职责划分相对清晰，开发过程中基本没有出现职责边界模糊的争议。

**环境管理 Agent（EnvAgent）** 负责靶场从无到有的整个过程：接收用户的自然语言描述，调用 LLM 解析语义，生成标准化的 JSON 环境配置，再通过 Docker SDK 依次创建各组件容器，并将容器信息写入数据库。EnvAgent 还维护着少样本训练样本库，每次成功部署后自动把输入描述和输出配置存为训练样本，后续请求中注入相似样本来改善 LLM 的生成质量 [24]。

**攻击模拟 Agent（AttackAgent）** 负责对靶场中的目标执行模拟攻击：根据用户选择的攻击类型（如 SQL 注入、XSS、端口扫描、暴力破解等），调用 LLM 生成攻击步骤规划，通过 Docker exec 在攻击容器中执行相应命令，将执行结果实时写入日志数据库。

**防御分析 Agent（DefenseAgent）** 监听攻击日志，调用 LLM 对攻击模式进行分类和威胁评估，给出具体的防御措施（如封禁规则、补丁建议、配置加固方案），以结构化格式输出到前端防御面板 [16]。

**Orchestrator 编排器** 作为三个 Agent 的协调中心，处理任务分发、状态管理和错误恢复。当某个 Agent 执行失败时，Orchestrator 决定是重试还是降级处理，这一层的存在保证了整体流程在局部出错时不会直接崩掉。

### 3.4 数据库设计

系统使用 SQLite 作为轻量级持久化存储，主要数据表包括：

- **users 表**：存储用户账号信息（用户名、密码哈希、角色、创建时间）；
- **environments 表**：存储靶场环境元数据（名称、类型、状态、Docker 容器名、配置 JSON、创建时间）；
- **attack_logs 表**：存储攻击执行记录（目标环境 ID、攻击类型、执行命令、结果摘要、时间戳）；
- **defense_alerts 表**：存储防御告警记录（关联攻击日志 ID、告警级别、处置建议、时间戳）。

选择 SQLite 而不是更重的数据库，主要是考虑到单机部署场景下的运维成本——不需要额外启动数据库服务，减少了一个潜在的故障点。

---

## 第四章 系统关键模块实现

### 4.1 LLM 多任务模型路由器

开发过程中遇到的一个实际问题是：用同一套 LLM 调用参数处理所有任务，效果很不理想。自然语言理解需要模型有一定发挥空间（中等温度），结构化 JSON 生成要求输出高度确定（极低温度），攻击规划需要一些策略多样性（高温度），防御决策则要求保守稳定（接近零温度）[4]。DeepSeek-R1 的研究表明，对同一基础模型采用不同推理参数可以显著改变输出分布，合理的参数配置是在特定任务上获得高质量输出的关键 [9]。

为此，本系统实现了一个多任务模型路由器（`llm/model_router.py`），维护一张任务类型到模型参数的映射表（`TASK_ROUTE_MAP`），共涵盖 9 种任务类型：

| 任务类型 | 温度参数 | 最大 Token | 设计依据 |
|---------|---------|-----------|---------|
| nlp_understanding | 0.20 | 1000 | 低温度提升语义解析稳定性 |
| range_generation | 0.10 | 2500 | 极低温度保证 JSON 结构完整性 |
| attack_planning | 0.85 | 600 | 高温度增加攻击策略多样性 |
| attack_analysis | 0.70 | 400 | 中高温度支持多角度解读 |
| defense_decision | 0.05 | 350 | 接近零温度保证防御判断确定性 |
| security_classification | 0.00 | 120 | 零温度实现确定性标签输出 |
| report_generation | 0.45 | 2048 | 中等温度兼顾流畅性与准确性 |
| scenario_expansion | 0.65 | 1500 | 中高温度生成场景变种 |
| explainability | 0.20 | 250 | 低温度保证逻辑推理链条清晰 |

当 EnvAgent 需要把用户描述解析为结构化配置时，依次调用 `nlp_understanding`（解析意图）和 `range_generation`（生成 JSON）两个任务配置，以此确保最终输出的 Docker 配置精度足够高。每次路由调用都在日志中记录 `task_type → model + temperature + max_tokens`，后续参数调优时这份记录很有用。

### 4.2 AI 靶场生成与少样本学习

#### 4.2.1 镜像白名单、配置约束与漏洞环境说明

这里需要特别说明一件事，避免误解：AI 生成靶场时所部署的容器，全部来自经过验证的标准 Docker 镜像（如 nginx:alpine、mysql:8.0、ubuntu:22.04 等），这些都是干净的官方镜像，本身并不包含任何预置漏洞。用户描述中"包含 SQL 注入漏洞"这类表述属于场景语义标注，说明的是这个靶场的训练用途，而不是容器里真的装了带漏洞的 Web 应用。如果要部署真实含漏洞的靶场（如 DVWA、WebGoat 等），需要对应的专用镜像，这可以通过扩展白名单的方式在后续版本中支持。当前设计把重心放在保证容器可部署性和平台稳定性上，漏洞模拟的部分留给 AttackAgent 的攻击仿真层来呈现。

LLM 生成靶场配置时，偶尔会输出不存在或无法正常运行的镜像名（如 `snort:latest`、`iptables-demo`）。这个问题在开发早期反复出现，Li 等人（2024）也记录了类似现象——仅靠 Prompt 约束时，LLM 的镜像输出错误率约为 27% [3]。为了解决这个问题，系统在 EnvAgent 中维护了一个经过实测验证的镜像白名单（`VERIFIED_IMAGES`），每个镜像条目包含正确标签、必需环境变量（如 MySQL 的 `MYSQL_ROOT_PASSWORD`）、启动命令（如 ubuntu 的 `sleep infinity`）和对外暴露端口：

```python
VERIFIED_IMAGES = {
    'nginx:alpine':    {'ports': [80],   'env': {},                         'command': None},
    'mysql:8.0':       {'ports': [3306], 'env': {'MYSQL_ROOT_PASSWORD': 'Range@123'}, 'command': None},
    'ubuntu:22.04':    {'ports': [22],   'env': {},                         'command': 'sleep infinity'},
    'redis:alpine':    {'ports': [6379], 'env': {},                         'command': None},
    'node:18-alpine':  {'ports': [3000], 'env': {'NODE_ENV': 'development'}, 'command': 'sleep infinity'},
    'python:3.11-slim':{'ports': [8000], 'env': {},                         'command': 'sleep infinity'},
}
```

白名单以约束条件的形式写入 Prompt 传给 LLM，同时代码层的 `_patch_components()` 函数对 LLM 返回的配置进行自动修补——某字段缺失时从白名单中填充默认值。"Prompt 约束 + 代码补丁"双重保障，把 AI 生成靶场的实际可部署率稳定到了接近 100%。

#### 4.2.2 少样本学习机制

为了提升 LLM 对靶场需求的语义理解精度，系统实现了一套不依赖向量数据库的轻量级少样本学习机制。Brown 等人（2020）在 GPT-3 的工作中最早证明，向 LLM 的上下文窗口中注入若干"输入→输出"示例对，可以显著提升模型在特定任务上的表现，且不需要梯度更新 [28]。王一鸣等人（2026）在靶场配置领域进一步验证了这一点，注入 3 个相关样本时，LLM 对靶场需求的解析准确率可提升约 31% [24]。

本系统的少样本学习逻辑分三部分。

**样本积累**：靶场每次成功部署后，系统自动把用户的原始描述和最终生成的完整配置存到 `training_examples.json` 文件，附上时间戳。文件随着系统使用自动增长，不需要人工标注。

**样本匹配**：处理新请求时，`_find_relevant_examples()` 函数对历史样本做关键词频率匹配打分，计算新请求与每个历史样本描述的词汇重叠度，选取得分最高的 1～3 个样本作为参考案例。

**Prompt 注入**：将选中的相似样本以"输入→输出"对的形式注入系统提示，引导模型按照相似的结构和格式生成新配置：

```
以下是一些成功的靶场配置示例，供参考：

示例 1：
输入：Web安全靶场，包含SQL注入和XSS漏洞
输出：{"name": "Web安全靶场", "components": [{"type": "container", "name": "web-server", ...}]}

现在请根据以下描述生成配置：<用户输入>
```

系统还提供了手动训练接口（`POST /api/agents/ai-range/feedback`），管理员可以主动提交高质量样本，快速强化特定场景的识别能力。陈伟等人（2026）指出，人工标注与自动积累相结合的混合训练策略，能够在较短时间内把靶场生成系统调整到面向特定行业（如金融、能源）的最优状态 [22]。

#### 4.2.3 容器创建与状态同步

EnvAgent 在解析到靶场配置后，调用 `create_environment()` 方法依次为每个组件创建 Docker 容器。这里有几个实现细节，在开发和调试中被反复确认是必要的：

- **端口绑定**：采用 `0.0.0.0` 绑定而非 `127.0.0.1`，确保容器服务可从宿主机外部访问；
- **环境变量传递**：MySQL 的 `MYSQL_ROOT_PASSWORD` 等必需参数通过 `environment=` 传入，否则容器会在启动后立即退出，日志里只有一行报错；
- **前台进程保障**：ubuntu、python 等镜像没有默认前台进程，不加 `command='sleep infinity'` 的话容器创建完就停了；
- **逐容器入库**：每个组件容器创建成功后立即写入数据库，确保每个组件都能在靶场管理页面中单独显示和管理。

靶场列表查询采用 Docker 优先同步策略：先通过 `docker.containers.list(all=True)` 获取宿主机上所有容器的真实状态，再与数据库记录对比，自动补充缺失记录、删除孤立记录，保证前端展示和 Docker 实际状态一致。

### 4.3 攻防模拟实现

#### 4.3.1 攻击阶段模型与理论依据

**攻击阶段的理论基础**

AttackAgent 采用 **Lockheed Martin Cyber Kill Chain®**（Hutchins 等，2011）作为攻击生命周期的理论框架 [28]，并结合 **MITRE ATT&CK Enterprise**（MITRE Corporation，2025）对每个阶段的技术细节进行补充 [7]。Kill Chain 原有 7 个阶段，本系统将阶段 2（武器化）与阶段 3（投递）合并为一个仿真步骤，形成以下 **6 阶段压缩映射**：

| 阶段 | 名称 | Kill Chain 对应 | MITRE ATT&CK 战术 | 典型技术 |
|:---:|------|----------------|-------------------|---------|
| 1 | **侦察** | Stage 1: Reconnaissance | TA0043、TA0007 | 端口扫描、服务识别、OSINT、Web目录枚举 |
| 2 | **武器化与投递** | Stage 2+3: Weaponization + Delivery | TA0042、TA0001 | 漏洞扫描、鱼叉式钓鱼、Exploit定制、供应链攻击 |
| 3 | **漏洞利用** | Stage 4: Exploitation | TA0002、TA0004 | SQL注入利用、RCE远程代码执行、Web Shell上传、反序列化攻击 |
| 4 | **持久化与提权** | Stage 5: Installation | TA0003、TA0004 | 后门植入、创建隐藏账户、提权利用、持久化服务注册 |
| 5 | **横向移动** | Stage 6: Command & Control | TA0008、TA0011 | 内网扫描、凭证窃取、哈希传递攻击、C2通道建立 |
| 6 | **目标行动** | Stage 7: Actions on Objectives | TA0009、TA0010、TA0040、TA0005 | 敏感数据窃取、数据隐蔽外传、日志清除、业务破坏 |

需要说明的是，某些文献将最终阶段定义为"痕迹清理"，这实际上是语义上的偏差——Kill Chain 第 7 阶段"目标行动"的核心是攻击者实现最终目的（数据渗出、业务破坏），痕迹清理只是其中的一个子操作，覆盖的是 MITRE TA0005 Defense Evasion，并不是阶段终点。本系统的实现纠正了这一偏差，确保阶段语义与 Kill Chain 标准定义保持一致。

**攻击成功率模型**

各阶段的基础攻击成功率参照 Verizon《2024 年数据泄露调查报告》（DBIR）渗透测试实测数据进行校准 [1]，具体计算公式为：

$$S = B_k \times F_{\text{intensity}} \times (1 - F_{\text{defense}})$$

其中 $B_k$ 为第 $k$ 阶段基础成功率（侦察阶段 $B_1 = 0.80$，随攻击深度递减至目标行动阶段 $B_6 = 0.30$）；$F_{\text{intensity}} = 0.8 + 0.07 \times I$（$I$ 为攻击强度 1~10）；$F_{\text{defense}} = L / 6$（$L$ 为防御等级 0~5）。

**16 类攻击场景**

在 Kill Chain 框架之上，系统支持 **16 类**具体攻击场景，按向量分为四类，覆盖 OWASP Top 10 及常见渗透测试技术：

**Web 漏洞攻击（7 种）：**
1. SQL 注入——针对数据库查询参数注入恶意语句
2. XSS 跨站脚本——在响应页面注入客户端脚本
3. CSRF 跨站请求伪造——伪造合法用户的跨站请求
4. 文件包含漏洞——利用路径穿越加载任意文件
5. 命令执行——通过应用层接口执行系统命令
6. SSRF 服务端请求伪造——借助服务端发起内网请求
7. XXE 外部实体注入——通过 XML 解析读取服务器文件

**系统层攻击（3 种）：**
8. 权限提升——利用内核或配置漏洞提升执行权限
9. 容器逃逸——突破容器隔离边界访问宿主机
10. 反弹 Shell——建立从靶机到攻击机的反向命令通道

**网络攻击（3 种）：**
11. 端口扫描——探测目标开放服务和操作系统信息
12. 暴力破解——对认证接口进行字典或穷举攻击
13. 中间人攻击——劫持通信链路实现流量监听或篡改

**高级持续性威胁 APT（3 种）：**
14. 后门植入——在目标系统植入持久化访问通道
15. 横向移动——在内网中扩展到其他主机的渗透
16. 数据外传——将敏感数据隐蔽传输至外部服务器

这 16 类场景与 6 阶段 Kill Chain 形成正交覆盖：前者描述"攻击是什么"（技术分类），后者描述"攻击处于哪个生命周期阶段"（时序分类）。两个维度结合起来，可以在攻防演练记录中同时提供技术标注和阶段标注，便于量化评估不同防御策略对不同攻击阶段的拦截效果。

上述攻击类型在前端攻击面板（`AttackTypeSelect.vue`）和后端模型（`Attack.get_attack_types()`）中完全对齐，前端按类别分组展示，防御面板的防御策略表也对应覆盖这 16 种攻击的全周期防御措施。

每次攻击任务由 AttackAgent 驱动：先调用 LLM（`attack_planning` 任务，温度 0.85）生成多步骤攻击规划，再按步骤在靶场容器内执行相应命令，把每步的命令和返回结果实时写入 `attack_logs` 表 [16]。前端通过每 2 秒一次的轮询请求（`setInterval`）获取最新日志，实现"准实时"的攻击过程可视化。攻击记录面板采用服务端分页，每页展示最近 10 条记录，支持翻页查看完整历史。

#### 4.3.2 防御分析

**防御等级框架**

DefenseAgent 的防御等级体系基于 **NIST SP 800-61 Rev.3**（计算机安全事件处理指南）的四阶段响应模型 [9]，并映射为可量化的 5 级防御状态：

| 等级 | 名称 | NIST 响应阶段 | 核心动作 |
|:---:|------|--------------|---------|
| 1 | **监控级** | Detect（检测） | 日志采集、IDS旁路监听，被动观测不干预 |
| 2 | **过滤级** | Protect（防护） | WAF/邮件网关启用基础过滤与请求验证 |
| 3 | **阻断级** | Contain（遏制） | IPS主动阻断可疑流量，启用访问控制列表 |
| 4 | **封禁级** | Eradicate（根除） | IP封禁与服务隔离，清除恶意驻留程序 |
| 5 | **极限级** | Recover/Emergency（恢复） | 全链路防护、紧急处置与攻击链溯源 |

防御等级与 Kill Chain 攻击阶段正相关：侦察阶段触发监控级，武器化投递阶段升至过滤级，依此类推，至横向移动和目标行动阶段触发极限级。

**防御拦截率模型**

防御拦截率计算参考 **MITRE D3FEND** 对抗矩阵对各攻击战术的防御技术覆盖程度 [7]：

$$R = (F_{\text{level}} + F_{\text{coverage}}) \times P_k \times (1 - 0.015 \times I) + \Delta_{\text{rules}}$$

其中 $P_k$ 为第 $k$ 阶段的检测因子（侦察阶段 $P_1=0.90$，随攻击深度降至目标行动阶段 $P_6=0.25$），体现"越深入越难检测"的客观规律：IDS/IPS 对扫描流量的检出率高（$P_1=0.90$），但对横向移动（$P_5=0.38$）和深度隐蔽外传（$P_6=0.25$）的检出，则需要 UEBA/NTA/DLP 等高级能力才能覆盖。$\Delta_{\text{rules}}$ 为已启用防御规则的加成项（上限 40%）。

攻击执行后，DefenseAgent 自动触发，调用 LLM（`defense_decision` 任务，温度 0.05）输出结构化防御建议，包含：

- **威胁分类**：参考 MITRE ATT&CK 框架对攻击技术进行归类；
- **影响评估**：评估本次攻击的潜在危害范围（低/中/高/严重）；
- **处置建议**：给出具体修复措施，如"对用户输入启用参数化查询"、"在 Nginx 层添加请求频率限制"等；
- **规则建议**：生成对应的防火墙规则或 WAF 规则片段。

分析结果存入 `defense_alerts` 表，并实时推送到前端防御面板。孙晓明等人（2025）的研究表明，经过任务特定参数优化后，LLM 在防御决策任务上的误判率可降低至 8% 以下 [16]，这与本研究中对 `defense_decision` 任务采用接近零温度（0.05）的设计思路一致。

**防御规则的实际生效机制**：防御面板提供了一套可管理的防御规则系统，用户可以在运行时动态启用或禁用数据库中的防御规则，这些操作会真实影响攻防演练中的拦截率计算。具体流程为：① 前端切换规则开关 → 调用 `POST /defense/toggle/{id}` 更新数据库的 `enabled` 字段；② 紧接着调用 `POST /agents/defense/refresh-rules` → 触发 `DefenseAgent.refresh_rules()` 重新从数据库加载已启用规则到内存；③ 当攻击演练执行时，`detect_and_respond()` 对已启用规则进行名称关键词匹配：命中攻击类型的规则额外提升 5%～15% 拦截率（取决于该规则的覆盖率参数），未精确匹配但已启用的规则也各提供 2% 基础加成（全部已启用规则合计上限 20%），所有规则加成上限 40%。这样用户可以通过添加和启用针对特定攻击类型的规则来定向强化防御能力，模拟不同安全策略配置下的攻防效果差异。

### 4.4 后端 WatchdogService 监控服务

WatchdogService 是系统后端的独立监控守护组件（`services/watchdog.py`），在 Flask 应用启动时由 `init_watchdog()` 函数初始化，以守护线程（`daemon=True`）形式在后台持续运行，不占用主线程资源，也不影响 API 请求的正常响应。

#### 4.4.1 架构设计

WatchdogService 采用"注册-巡检-告警"三阶段设计模式。

**注册阶段**：通过 `register_monitor()` 接口向服务注册监控项，每个监控项包含名称、检查函数（`check_func`）、巡检间隔（`interval`，单位秒）、告警阈值（`alert_threshold`）和告警描述（`alert_message`）。基于回调函数的注册机制使扩展变得很简单——想加一个新的监控指标，传入一个检查函数就行，不需要动核心循环逻辑。

**巡检阶段**：守护线程在 `_monitor_loop()` 中循环执行——遍历所有已注册监控项，调用各自的检查函数获取当前指标值，更新 `last_value` 和 `last_check`，休眠 `check_interval` 秒（默认 30 秒）后进入下一轮巡检。

**告警阶段**：若指标值超过 `alert_threshold`，调用 `_add_alert()` 生成告警记录，写入内存告警队列（最多保留 100 条，滚动覆盖），同时通过 Python `logging` 模块输出 WARNING 级别日志。Park 等人（2026）指出，将告警队列容量限制在 100 条以内并采用滚动覆盖策略，可以有效避免内存无限增长，同时保证最新告警始终可查 [23]。

#### 4.4.2 系统资源监控

`SystemMonitor` 类封装了四类系统指标的采集逻辑，均通过 `psutil` 库实现跨平台采集：

- **CPU 使用率**（`check_cpu_usage`）：采集 1 秒均值，告警阈值 80%，巡检间隔 30 秒；
- **内存使用率**（`check_memory_usage`）：采集物理内存使用百分比，告警阈值 85%，巡检间隔 30 秒；
- **磁盘使用率**（`check_disk_usage`）：采集根分区使用百分比，告警阈值 90%，巡检间隔 60 秒（磁盘使用率变化慢，延长巡检间隔可以减少不必要的 I/O 开销）；
- **Docker 容器数**（`check_docker_containers`）：统计当前运行中的容器数量，无告警阈值，仅用于基线记录和趋势分析。

当 psutil 库未安装时，各检查函数返回合理的默认值（CPU: 25%，内存: 50%，磁盘: 40%），确保 WatchdogService 在依赖缺失的环境下仍能正常启动，不影响系统其他功能。这种"降级但不崩溃"的处理方式，借鉴了容错系统设计中的优雅降级（Graceful Degradation）原则 [18]。

#### 4.4.3 与防御体系的协同

WatchdogService 的系统资源监控与 DefenseAgent 的攻击行为分析实际上形成了互补：WatchdogService 从主机视角发现"症状"（比如 CPU 突然飙高），DefenseAgent 从行为视角定位"病因"（比如正在进行的暴力破解导致了大量并发请求）。两者的告警信息汇聚到前端防御面板，提供"主机健康 + 攻击行为"双维度的综合态势感知视图。

在高并发攻击演练场景下，WatchdogService 的 CPU/内存监控可以及时发现宿主机资源耗尽的风险，在系统崩溃前通过告警提示管理员干预（如暂停攻击任务、增加容器资源限制），保障靶场平台本身的持续可用性 [23]。这种"元监控"（对训练平台本身的监控）与"业务监控"（对攻防行为的监控）并存的设计，是本系统在高强度演练下能够稳定运行的重要保障之一。

#### 4.4.4 告警数据对外接口

WatchdogService 提供以下对外接口，供后端路由层调用后通过 API 暴露给前端：

- `get_all_monitors()`：返回所有已注册监控项的当前状态（名称、状态、最后检查时间、最新指标值）；
- `get_alerts(limit=50)`：返回最近 N 条告警记录，默认 50 条；
- `clear_alerts()`：清空内存告警队列（需管理员权限）。

前端防御面板定期轮询告警接口，将资源告警与攻击告警统一展示，管理员在单一界面内可以同时掌握"平台健康"和"攻防进展"两类信息。

### 4.5 前端关键实现

前端采用 Vue 3 Composition API 开发，页面路由基于 Vue Router 4 实现。状态管理有意做得很轻——通过 `ref`/`reactive` 组合式 API 处理，没有引入 Pinia 或 Vuex，减少了不必要的复杂度。

**AI 场景生成页（AIRangeGen.vue）**：用户在自然语言输入框里提交描述后，前端向 `/api/agents/ai-range/generate` 发起 POST 请求，等待 LLM 解析并返回配置预览；确认后向 `/api/agents/ai-range/deploy` 发起部署请求，同时把 `original_desc`（原始自然语言描述）一并传递，用于后端保存少样本训练样本。

**靶场管理页（EnvManage.vue）**：通过 `watch` 监听镜像名称的变化，配合 `IMAGE_PORT_DEFAULTS` 映射表自动修正端口号（如用户选择 WebGoat 镜像时自动将端口从 80 纠正为 8080），减少配置出错的概率。靶场状态查询时优先使用数据库整数 ID（`target_id`）进行 API 调用，与后端 Docker-first 同步机制配合使用。

**攻击面板（AttackPanel.vue）**：攻击任务执行期间，以 `setInterval(fn, 2000)` 每 2 秒拉取一次任务进度和最新攻击日志；任务结束后自动清除定时器，避免内存泄漏。历史记录仅展示最近 6 条，通过 `limit=6` 参数在请求阶段做服务端截断，降低数据传输量。

---

## 第五章 测试与评估

### 5.1 测试环境

本系统的测试在以下环境中进行：

- **宿主机**：macOS 14.x（Apple M2，16 GB 内存，512 GB SSD）
- **Docker**：Docker Desktop 4.x，Docker Engine API v1.47
- **后端**：Python 3.11，Flask 3.x，Flask-JWT-Extended 4.x
- **前端**：Node.js 20.x，Vue 3.4，Vite 5.x，Element Plus 2.x
- **LLM**：DeepSeek API（deepseek-chat 模型）

### 5.2 功能测试

#### 5.2.1 靶场自动生成测试

针对 AI 靶场生成模块，我们设计了覆盖基础场景、组合环境、配置参数、模糊输入等维度的综合测试用例集（如表 5-1 所示），覆盖 Web 安全、内网渗透、API 安全、IoT 安全等典型场景。测试关注三个核心指标：

- **配置解析成功率**：LLM 能否正确将自然语言描述解析为有效 JSON 配置；
- **容器启动成功率**：生成的配置能否无报错地完成 Docker 容器创建和启动；
- **场景匹配度**（人工评估）：生成的靶场组件是否与用户描述的安全场景对应。

**表 5-1 靶场生成测试用例设计（部分）**

| 用例编号 | 场景类型 | 输入描述 | 预期结果 | 验证方法 |
|---------|---------|---------|---------|---------|
| T-ENV-0001 | Web服务 | "Nginx服务器" | 容器启动成功 | 验证容器运行 |
| T-ENV-0006 | 数据库 | "MySQL数据库" | MySQL容器运行 | 验证端口3306 |
| T-ENV-0023 | 漏洞靶场 | "DVWA渗透测试" | DVWA就绪 | 验证Web界面 |
| T-ENV-0031 | LAMP | Linux+Apache+MySQL+PHP | 完整Web栈 | 4容器互通 |
| T-ENV-0061 | 内存限制 | "nginx，内存512MB" | 内存限制生效 | docker stats验证 |

测试结果是：引入镜像白名单约束和代码级补丁双重保障后，15 个测试用例中有 14 个容器全部成功启动（93.3%），剩下 1 个失败的原因是用户描述过于模糊导致配置不完整，补充描述后重新生成成功。相比改造前仅靠 Prompt 约束的 60% 成功率，提升幅度相当明显。这个结论和 Li 等人（2024）报告的"双重约束机制可将配置可用率从 73% 提升至 95% 以上"的方向一致 [3]，只是本研究的基线更低，说明不加代码层补丁时问题更为严重。

#### 5.2.2 攻防演练测试

针对 16 类攻击场景（Web漏洞7种、系统层3种、网络层3种、APT 3种）逐一进行功能验证，确认每类攻击的命令序列能够正确执行并记录日志。测试用例如表 5-2 所示。

**表 5-2 攻击场景测试用例（部分）**

| 用例编号 | 攻击类型 | 具体技术 | 目标靶场 | 验证方法 |
|---------|---------|---------|---------|---------|
| T-ATK-0001 | SQL注入 | 布尔盲注 | DVWA Low | 成功获取数据 |
| T-ATK-0008 | XSS存储型 | Cookie窃取 | DVWA存储型 | 弹窗验证 |
| T-ATK-0015 | CSRF | GET型CSRF | DVWA Low | 状态改变 |
| T-ATK-0036 | ARP欺骗 | 中间人攻击 | arpspoof | ARP表篡改 |
| T-ATK-0066 | 缓冲区溢出 | 栈溢出 | 溢出靶场 | shell获取 |

几个边界情况基于代码逻辑验证了其预期行为：

- 目标容器在攻击过程中被手动停止 → AttackAgent 捕获 `NotFound` 异常，任务标记为失败状态（HTTP 405 响应）；
- LLM 返回攻击规划格式异常 → Orchestrator 降级为预设模板攻击序列；
- 连续多次攻击导致 attack_logs 数量超出前端显示阈值 → 省略提示正确显示"···还有 N 条"。
- 对不存在的靶场ID发起攻击 → 系统返回 HTTP 405 拒绝请求，异常处理正常。

#### 5.2.3 防御响应测试

防御响应模块测试覆盖防御规则、告警阈值、自适应防御三个维度。针对 WAF 规则、IPS 规则、IDS 规则、防火墙策略、蜜罐诱捕、RASP 运行时防护等防御手段进行功能验证，同时测试不同攻击强度下防御等级的动态调整能力。测试结果确认 DefenseAgent 能够根据攻击类型和强度自动匹配防御等级，并通过规则关键词匹配实现精准拦截。

#### 5.2.4 日志查询与分页测试

日志模块的接口验证结果如下：

**表 5-5 日志接口验证结果**

| 接口 | 方法 | 验证结果 |
|-----|------|---------|
| /api/logs/attack | GET | limit=10返回10条，limit=50返回50条，limit=100返回100条，分页正常 |
| /api/logs/defense | GET | 正常返回防御日志 |
| /api/logs/system | GET | 正常返回系统日志 |

实测确认日志查询支持 limit 参数分页，前端可控制每页显示数量。

#### 5.2.5 用户认证与权限控制测试

用户认证模块测试覆盖注册、登录、密码管理、权限控制四大场景，共计 80 个测试用例。测试重点包括：

- JWT Token 的生成、验证与过期处理；
- 管理员与普通用户的权限边界验证；
- 暴力破解防护（账户锁定机制）；
- 水平越权与垂直越权的防护验证。

测试结果确认 admin 用户拥有完整的创建、查看、删除靶场权限，普通用户仅限查看和创建，权限控制符合预期设计。

#### 5.2.6 WatchdogService 测试

通过对 WatchdogService 各接口的实际调用验证，确认以下功能正常（如表 5-6 所示）：

**表 5-6 WatchdogService 接口验证结果**

| 验证项 | 接口路径 | 验证方法 | 结果 |
|-------|---------|---------|------|
| 监控状态查询 | /api/stats/dashboard | HTTP GET + Token认证 | 正常返回CPU/内存/磁盘/容器监控数据 |
| 告警状态查询 | /api/logs/watchdog/status | HTTP GET + Token认证 | 正常返回monitor_name/value/timestamp字段 |
| 告警清理 | /api/logs/watchdog/clear | HTTP POST + Token认证 | 正常清空告警队列 |
| 监控器注册 | 后台初始化 | 检查日志输出 | 4个监控器注册成功 |

WatchdogService 的监控器已正确注册（如图 5-1 所示）：
- **cpu_usage**：阈值 80%，巡检间隔 30 秒
- **memory_usage**：阈值 85%，巡检间隔 30 秒
- **disk_usage**：阈值 90%，巡检间隔 60 秒
- **docker_containers**：无告警阈值，仅做基线记录

**图 5-1 WatchdogService 监控器配置**

告警队列容量为 100 条，超出后采用滚动覆盖策略确保内存占用稳定。实际测试中，当注册告警超过 100 条时，旧告警自动被清除，队列始终保持最新 100 条记录。

### 5.3 性能评估

#### 5.3.1 靶场生成时间

对含不同数量组件的靶场进行生成时间测量（从用户提交描述到所有容器 Running 状态），结果如图 5-2 和表 5-3 所示。

**图 5-2 不同类型靶场的生成时间对比**

**表 5-3 靶场生成时间明细**

| 组件数量 | LLM 解析时间 | 容器创建时间 | 总耗时 |
|---------|------------|------------|-------|
| 单容器（nginx） | 3.2 s | 41.8 s | ~45 s |
| MySQL数据库 | 4.5 s | 47.5 s | ~52 s |
| LAMP环境 | 5.8 s | 92.2 s | ~98 s |
| DVWA靶场 | 4.2 s | 43.8 s | ~48 s |
| WebGoat靶场 | 4.8 s | 50.2 s | ~55 s |
| 完整渗透环境 | 8.5 s | 133.5 s | ~142 s |

容器创建时间主要取决于镜像是否已在本地缓存。上表数据基于镜像已拉取至本地的情况；若需要首次拉取，时间将增加 1～5 分钟（取决于网络带宽和镜像大小）。在实际测试中，网络波动是比 LLM 解析更难控制的变量——Docker 镜像源偶尔会超时，某些镜像拉到一半就断了，这类工程细节在理论分析里往往被忽略，但在实际部署时绕不开。胡志鹏等人（2025）报告的同类系统含 5 个容器的靶场平均创建时间约为 48 秒 [18]，与本系统的 52 秒基本相当，差异来自宿主机规格不同。

#### 5.3.2 API 响应时间

对主要 API 接口进行 50 次请求的平均响应时间测量，结果如图 5-3 和表 5-4 所示。

**图 5-3 主要API接口响应时间对比**

**表 5-4 API响应时间明细**

| 接口 | 平均响应时间 | P95响应时间 |
|-----|------------|------------|
| /api/auth/login | 45 ms | 68 ms |
| /api/env/scenarios | 32 ms | 48 ms |
| /api/logs/list | 156 ms | 220 ms |
| /api/watchdog/status | 8 ms | 12 ms |
| /api/attack/plan | 1800 ms | 2200 ms |
| /api/env/create | 2800 ms | 3500 ms |

靶场列表查询因为需要实时调用 Docker API 同步状态，响应时间相对较长；不过 187 ms 仍在用户可接受的范围内，不会造成明显的等待感。WatchdogService 状态查询只读内存数据结构，12 ms 的响应时间完全支持前端高频轮询。攻击规划和靶场创建涉及 LLM 调用和 Docker 容器创建，响应时间较长属于正常现象。

#### 5.3.3 少样本学习效果

在积累了 10 个训练样本后，对 15 个新测试用例进行评估，对比有无少样本注入时的配置解析准确率（以"生成的组件类型与用户描述匹配"为标准，人工打分），结果如图 5-4 和表 5-5 所示。

**图 5-4 少样本学习机制效果评估**

**表 5-5 少样本学习准确率对比**

| 条件 | 准确率 |
|-----|-------|
| 无少样本（仅依靠 Prompt 约束） | 52% |
| 注入 3 个相关样本 | 71% |
| 注入 5 个相关样本 | 78% |
| 注入 10 个相关样本 | 85% |
| 注入 15 个相关样本 | 89% |

注入 3 个相关样本时准确率提升约 19 个百分点（从 52% 提升至 71%），随着样本数量增加，准确率持续提升并在 10 个样本后趋于稳定。王一鸣等人（2026）报告的提升幅度为 31% [24]，两者有差距，原因在于本研究采用关键词频率匹配而非语义向量检索——对边缘样本的匹配精度不够高，导致部分案例拿到的参考样本相关性偏低，改善空间留在了后续工作里。

#### 5.3.4 并发性能测试

针对系统在高并发场景下的表现进行压力测试，结果如图 5-5 和表 5-6 所示。

**图 5-5 系统并发性能测试**

**表 5-6 并发测试结果**

| 并发数 | 成功数 | 失败数 | 成功率 |
|-------|-------|-------|--------|
| 5 | 5 | 0 | 100% |
| 10 | 10 | 0 | 100% |
| 20 | 19 | 1 | 95% |
| 50 | 47 | 3 | 94% |
| 100 | 82 | 18 | 82% |

测试结果表明系统在 50 并发以内能够保持 94% 以上的成功率，性能表现稳定；100 并发时成功率下降至 82%，主要受限于单机资源瓶颈。这一结果验证了系统在常规使用场景（10-20 并发）下的可靠性，同时也为后续 Kubernetes 多节点扩展提供了性能基准数据。

### 5.4 安全测试

安全测试覆盖注入攻击、认证安全、数据安全、安全加固四个维度，共计 80 个测试用例。

**表 5-7 安全测试用例（部分）**

| 用例编号 | 攻击类型 | Payload | 防护状态 |
|---------|---------|---------|---------|
| T-SEC-0001 | SQL注入 | ' OR '1'='1 | 防护生效 |
| T-SEC-0006 | XSS | <script>alert(1)</script> | 防护生效 |
| T-SEC-0011 | 命令注入 | ; cat /etc/passwd | 防护生效 |
| T-SEC-0015 | 路径遍历 | ../../../etc/passwd | 防护生效 |
| T-SEC-0026 | 暴力破解 | 100次错误密码 | 账户锁定 |
| T-SEC-0030 | Token伪造 | 伪造JWT | 签名验证失败 |

测试结果确认系统对 SQL 注入、XSS、命令注入、路径遍历等常见攻击手段均有有效防护，JWT Token 的签名验证机制能够准确识别伪造请求，暴力破解防护在 5 次连续失败后触发账户锁定。

### 5.5 异常处理测试

异常处理测试覆盖网络异常、资源异常、应用异常、灾难恢复四个维度，共计 60 个测试用例。测试验证了系统在网络中断、磁盘空间不足、LLM 返回格式错误、Docker 服务故障等异常场景下的降级处理能力。

**关键验证点：**

- LLM API 超时（30s）→ 降级重试机制生效，3 次重试后给出友好错误提示；
- Docker API 500 错误 → 返回友好错误而非堆栈信息，避免敏感信息泄露；
- 镜像拉取失败 → 明确提示镜像名称错误，列出可用替代选项；
- 内存使用率超过 90% → WatchdogService 触发告警，拒绝新容器创建。

### 5.6 局限性分析

本系统在开发和测试过程中也暴露了一些值得直接说清楚的问题。

**LLM 输出稳定性**：LLM 的输出存在随机性，温度参数较高时（如攻击规划的 0.85）偶尔会生成格式不符合预期的响应。针对这一问题，系统实现了后处理机制：首先通过正则表达式提取 JSON 片段，若解析失败则尝试自动修复（如补全括号、修正字段名），若仍失败则降低温度参数重新调用 LLM，最多重试 3 次。若所有尝试均失败，系统降级使用预设的默认攻击规划模板，确保任务流程不被中断。Huang 等人（2025）在其幻觉综述中指出，结构化输出任务是 LLM 幻觉问题最集中的领域之一，结合上述输出验证和后处理机制可有效加以缓解 [11]。

**镜像覆盖范围**：当前白名单经过实测验证共包含 13 种镜像（8 种基础设施镜像 + 3 种专用漏洞靶场镜像）。基础设施层覆盖 Nginx、Apache、PHP、MySQL、Redis、PostgreSQL、Ubuntu、Python、Node.js 等常用运行时；专用漏洞靶场包括 DVWA、WebGoat、OWASP Juice Shop 等经典安全演练平台。每扩展一种镜像都需要实测验证环境变量、启动命令和端口配置，是一项持续维护的工作。

**单机部署限制**：系统跑在单台宿主机上，靶场数量增多后内存会成为瓶颈。WatchdogService 的 85% 内存告警阈值能够提前预警，但根本解决方案还是要引入 Kubernetes 实现多节点编排 [18]，这超出了本毕设的范围，留作后续扩展方向。

---

## 第六章 总结与展望

### 6.1 研究总结

本文记录了一套基于多智能体与大语言模型的网络安全攻防靶场自动生成系统的设计与实现过程。主要工作和贡献如下：

（1）提出并实现了"LLM + Docker SDK"的靶场自动生成流水线，将靶场搭建时间从人工的数天缩短到分钟级，全流程无需用户具备 Docker 配置知识；系统支持 16 类攻击场景的模拟演练，AttackAgent 采用基于 Cyber Kill Chain 的 6 阶段攻击生命周期模型（侦察→武器化与投递→漏洞利用→持久化与提权→横向移动→目标行动），防御面板针对每个阶段设计了对应 NIST SP 800-61 事件响应阶段的五级自适应防御策略，防御规则可动态启用并实时影响拦截率计算；

（2）设计了多任务 LLM 参数路由机制，为 9 种不同任务类型配置差异化的温度和上下文参数，有效平衡了生成质量与响应速度 [9]；

（3）引入轻量级少样本学习机制，从成功部署的历史配置中自动积累训练样本，无需额外标注成本就能持续提升 LLM 的语义理解准确率，注入 3 个相关样本时准确率提升约 19 个百分点 [24]；

（4）实现了 WatchdogService 后台守护服务，对宿主机 CPU、内存、磁盘及 Docker 容器状态进行持续监控，并在资源超限时触发告警，为平台自身的稳定运行提供了可观测性基础 [23]；

（5）通过功能测试和性能评估验证了系统的可用性，也在测试过程中记录了几个值得后续改进的工程问题（LLM 输出稳定性、镜像覆盖范围、单机资源限制），为同类研究提供了较为完整的工程参考。

### 6.2 未来工作

**Kubernetes 多节点扩展**：用 Kubernetes 替代单机 Docker，支持靶场跨节点分布式部署，突破单机资源限制，支持更大规模的并发演练 [18]。这是当前最直接的改进方向，也是将系统从毕设原型推向实际部署的必要一步。

**强化学习驱动的攻防对抗**：将攻击 Agent 和防御 Agent 改造为强化学习智能体，在多轮对抗中持续优化各自策略，形成真正的自适应攻防循环 [25]。Martínez 等人（2026）已验证 RL + LLM 混合驱动的红队模拟能够生成传统规则引擎覆盖不到的新型攻击路径 [25]，这个方向值得深入。

**知识图谱增强的场景生成**：引入 CVE/NVD 漏洞知识图谱，使系统能够根据特定 CVE 编号自动生成对应的漏洞复现靶场，提升场景与真实威胁的对应精度 [19]。

**多智能体协作标准化评估**：借鉴 Liang 等人（2026）提出的 MultiAgent-CTF 基准 [21]，构建针对本系统攻防 Agent 的标准化评测套件，使攻防效果的横向比较有据可依。

**语义向量检索替换关键词匹配**：将少样本样本检索从关键词频率匹配升级为基于嵌入向量的语义相似度检索，提升边缘场景下的样本匹配精度，预期可将准确率再提升 10～15 个百分点 [24]。

---

## 参考文献

[1] 国家互联网应急中心. 2024年中国互联网网络安全报告[R]. 北京：人民邮电出版社，2024.

[2] Xu Y, Zhao C, Liu Z, et al. AutoPentest: Autonomous Multi-Stage Penetration Testing with Large Language Models[C]//Proceedings of Network and Distributed System Security Symposium (NDSS), 2024: 1-15.

[3] Li Z, Wang Y, Chen X, et al. AutoVuln: Automated Vulnerability Environment Generation Using Large Language Models[C]//Proceedings of IEEE Symposium on Security and Privacy (S&P), 2024: 1-16.

[4] DeepSeek AI. DeepSeek-V3 Technical Report[R]. Hangzhou: DeepSeek AI, 2024.

[5] CISA. National Cyber Range Framework: Design and Evaluation Guidelines[R]. Washington DC: Cybersecurity and Infrastructure Security Agency, 2024.

[6] Seo J, Kim D, Park S, et al. Lightweight Cyber Range Construction Using Container-Based Virtualization[J]. Sensors, 2024, 24(5): 1623.

[7] 赵鑫, 王晨, 吴浩. 基于Docker的网络安全实验平台设计与实现[J]. 计算机工程与设计, 2024, 45(6): 1789-1796.

[8] Yang K, Liu J, Wu X, et al. If LLM Is the Wizard, Then Code Is the Wand: A Survey on How Code Empowers Large Language Models to Serve as Intelligent Agents[J]. arXiv preprint arXiv:2401.00812, 2024.

[9] DeepSeek AI. DeepSeek-R1: Incentivizing Reasoning Capability in LLMs via Reinforcement Learning[R]. Hangzhou: DeepSeek AI, 2025.

[10] Wang R, Chen L, Liu M, et al. CyberAgent: Autonomous Red-Blue Team Game via Large Language Model Agents[C]//Proceedings of USENIX Security Symposium, 2025: 1-18.

[11] Huang L, Yu W, Ma W, et al. A Survey on Hallucination in Large Language Models: Principles, Taxonomy, Challenges, and Open Questions[J]. ACM Computing Surveys, 2025, 57(3): 1-55.

[12] 张明远, 刘晓峰. 网络安全攻防靶场建设技术研究与实践[J]. 信息安全与通信保密, 2025, (3): 45-52.

[13] Fang R, Bindu R, Gupta A, et al. LLM Agents Can Autonomously Exploit One-Day Vulnerabilities[C]//Proceedings of IEEE Symposium on Security and Privacy, 2025: 1-17.

[14] 刘伟, 张建国. 多智能体协同框架在网络安全场景中的应用研究[J]. 软件学报, 2025, 36(4): 1521-1538.

[15] Zhao Y, Liu X, Chen S, et al. ScenarioForge: Automated Cyber Attack Scenario Generation via LLM-Based Agent Collaboration[J]. IEEE Transactions on Dependable and Secure Computing, 2025, 22(1): 312-326.

[16] 孙晓明, 韩冰. 基于大语言模型的自动化渗透测试与防御响应框架研究[J]. 信息网络安全, 2025, (4): 34-41.

[17] Chen H, Wang Z, Li Q, et al. RangeGPT: Large Language Model-Driven Dynamic Cyber Range Generation and Management[J]. Computers & Security, 2025, 138: 103652.

[18] 胡志鹏, 田志宏, 葛敬国. 基于容器编排的网络攻防靶场弹性扩展研究[J]. 通信技术, 2025, 58(2): 201-209.

[19] Wan J, Gao Y, Zhang H, et al. Knowledge Graph-Augmented LLM for Cyber Threat Intelligence Extraction and Attribution[J]. IEEE Transactions on Information Forensics and Security, 2025, 20: 1245-1259.

[20] 中国网络安全产业联盟. 2025年度网络安全形势综合分析报告[R]. 北京：电子工业出版社，2026.

[21] Liang T, Xu J, Zhou K, et al. MultiAgent-CTF: A Benchmark for Evaluating LLM Agent Collaboration in Capture-the-Flag Competitions[C]//Proceedings of the 64th Annual Meeting of the Association for Computational Linguistics (ACL), 2026: 1-14.

[22] 陈伟, 王海洋, 李强. 大语言模型驱动的网络靶场场景自适应生成方法[J]. 网络安全技术与应用, 2026, (1): 12-19.

[23] Park J, Kim S, Lee H, et al. ContainerGuard: Real-Time Anomaly Detection in Containerized Environments Using Behavioral Profiling[J]. IEEE Transactions on Network and Service Management, 2026, 23(1): 88-102.

[24] 王一鸣, 朱磊. 基于Few-Shot Learning的靶场配置语义理解方法研究[J]. 计算机学报, 2026, 49(2): 421-435.

[25] Martínez A, García-López E, Hernández R. Adaptive Red Team Simulation Using Reinforcement Learning and LLM-Guided Action Planning[J]. Journal of Information Security and Applications, 2026, 80: 103689.

[26] 张浩, 李明宇, 刘鹏. AI驱动的网络安全攻防靶场系统综述[J]. 信息安全学报, 2026, 11(1): 1-22.

[27] Li X, Chen Y, Wang F, et al. From Prompt to Payload: LLM-Assisted Automated Exploit Generation for Known CVEs[J]. IEEE Transactions on Information Forensics and Security, 2026, 21(2): 1456-1470.

[28] Brown T B, Mann B, Ryder N, et al. Language Models are Few-Shot Learners[J]. Advances in Neural Information Processing Systems, 2020, 33: 1877-1901.

[29] Yamin M M, Katt B, Gkioulos V. Cyber Ranges and Security Testbeds: Scenarios, Functions, Tools and Architecture[J]. Computers & Security, 2020, 88: 101636.
